{
  "name": "webterm-tauri",
  "version": "1.0.0",
  "description": "WebTerm Tauri Application",
  "main": "index.html",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:fe": "cd ../fe && npm run build",
    "build:be": "cd ../be && go build -o ../desktop-tauri/src-tauri/binaries/webterm-backend-x86_64-pc-windows-msvc.exe ./cmd/server/main.go"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
