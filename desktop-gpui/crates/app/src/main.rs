//! WebTerm desktop shell (GPUI).
//!
//! The real GPUI application entry arrives in plan 20-03; this binary exists
//! now so `gpui` / `gpui-component` versions are resolved and locked in
//! Cargo.lock (workspace pin anchor — plan 20-01 Task 1, version-compat check).

fn main() {
    println!("webterm-desktop: shell not yet implemented (arrives in plan 20-03)");
}
