# Plan: Simplify Sidebar to Menu Only

Remove the host list (ConnectionList) from the sidebar as per user request. The sidebar should only contain navigation menus.

## Tasks
- [ ] Remove `ConnectionList` and its container from sidebar in `fe/src/App.tsx`
- [ ] Verify sidebar layout is clean and only contains navigation buttons
