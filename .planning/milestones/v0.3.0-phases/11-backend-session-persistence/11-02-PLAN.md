---
phase: 11-backend-session-persistence
plan: 11-02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [fe/src/lib/api.ts, fe/src/stores/app-store.ts, fe/src/App.tsx, fe/src/features/terminal/useSSHSession.ts, fe/src/features/terminal/types.ts, fe/src/features/terminal/components/TerminalPane.tsx]
autonomous: true
requirements: [SESS-03, SESS-04, SESS-06]

must_haves:
  truths:
    - "Frontend tabs are restored after page reload"
    - "Detached sessions automatically attempt to re-attach when focused"
    - "Terminal scrollback is recovered upon re-attachment"
  artifacts:
    - path: "fe/src/stores/app-store.ts"
      provides: "rehydrateSessions action for session recovery"
    - path: "fe/src/features/terminal/useSSHSession.ts"
      provides: "attach protocol implementation"
---

<objective>
Restore terminal tabs and re-attach to active SSH sessions on frontend reload, providing a seamless user experience that survives browser refreshes.

Purpose: Implement the frontend part of session persistence and re-attachment.
Output: Session hydration logic and re-attach UI flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-backend-session-persistence/11-01-SUMMARY.md
@fe/src/stores/app-store.ts
@fe/src/features/terminal/useSSHSession.ts
@fe/src/features/terminal/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Session Hydration in AppStore</name>
  <files>fe/src/lib/api.ts, fe/src/stores/app-store.ts, fe/src/features/terminal/types.ts</files>
  <action>
    Update the frontend to fetch active sessions on startup.
    - Update `fe/src/features/terminal/types.ts`: Add `detached` to `SessionStatus`.
    - Update `fe/src/lib/api.ts`: Add `getSessions()` to fetch from `GET /api/sessions`.
    - Update `fe/src/stores/app-store.ts`:
      - Add `rehydrateSessions` async action.
      - Implementation should:
        1. Call `api.getSessions()`.
        2. Map response to `SSHSession` objects with `status: 'detached'`.
        3. Set `sessions` array in store.
        4. If `activeSessionId` is null and sessions exist, set it to the first session.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Frontend store can be populated with active sessions from the backend.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Hydration into App Startup</name>
  <files>fe/src/App.tsx</files>
  <action>
    Trigger session hydration when the app mounts.
    - Update `fe/src/App.tsx` (or `fe/src/main.tsx` if more appropriate):
      - Use `useEffect` on mount to call `useAppStore.getState().rehydrateSessions()`.
      - Ensure this happens AFTER the `backendPort` is resolved (if using dynamic discovery).
  </action>
  <verify>
    <automated>Check browser console for /api/sessions call on reload</automated>
  </verify>
  <done>App fetches active sessions every time it is reloaded.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Re-attach Logic in Terminal Feature</name>
  <files>fe/src/features/terminal/useSSHSession.ts, fe/src/features/terminal/TerminalPane.tsx</files>
  <action>
    Support re-attaching to an existing session in the terminal hook and UI.
    - Update `fe/src/features/terminal/useSSHSession.ts`:
      - Add `attach()` function:
        - Creates WebSocket connection.
        - Sends `{ type: "attach", session_id: sessionId }` on open.
        - Handles messages same as `connect`.
      - Update `connect` to optionally take a `re-attach` flag or just detect if it should `attach`.
    - Update `fe/src/features/terminal/TerminalPane.tsx`:
      - Add a `useEffect` that checks if `session.status === 'detached'`.
      - If detached, call `attach()`.
      - Update UI to show "Resuming Session..." state when status is `connecting` and it's a re-attachment.
  </action>
  <verify>
    <automated>Connect to a server -> Refresh browser -> Verify tab restored and session re-attached with scrollback</automated>
  </verify>
  <done>Terminal automatically re-attaches to detached sessions on load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Backend | Fetching session list and sending attach messages |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | /api/sessions | mitigate | Ensure this API only returns metadata (Host/User), never credentials. |
</threat_model>

<verification>
- Start backend and frontend.
- Open multiple terminal tabs.
- Refresh the browser.
- Verify all tabs are restored.
- Verify active tab re-attaches immediately.
- Verify switching to other tabs triggers re-attachment and shows previous output.
</verification>

<success_criteria>
User can refresh the browser without losing their terminal sessions. All active connections are preserved and re-hydrated seamlessly.
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-session-persistence/11-02-SUMMARY.md`
</output>
