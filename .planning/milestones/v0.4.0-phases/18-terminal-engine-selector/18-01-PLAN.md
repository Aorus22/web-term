---
phase: 18-terminal-engine-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [be/internal/api/settings.go, fe/package.json, fe/src/features/settings/hooks/useSettings.ts, fe/src/features/terminal/types.ts, fe/src/features/terminal/useSSHSession.ts]
autonomous: true
requirements: [ENGINE-01, ENGINE-02]

must_haves:
  truths:
    - "Backend accepts 'terminal_engine' setting"
    - "Frontend dependencies for xterm.js are installed"
    - "useSSHSession is decoupled from @wterm/react"
  artifacts:
    - path: "be/internal/api/settings.go"
      provides: "terminal_engine setting support"
    - path: "fe/src/features/terminal/types.ts"
      provides: "TerminalHandle interface"
    - path: "fe/src/features/terminal/useSSHSession.ts"
      provides: "Engine-agnostic session management"
---

<objective>
Prepare the foundation for multiple terminal engines by updating the backend settings, installing necessary frontend dependencies, and decoupling the SSH session logic from specific terminal implementations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-terminal-engine-selector/18-RESEARCH.md
@be/internal/api/settings.go
@fe/src/features/terminal/useSSHSession.ts
@fe/src/features/terminal/types.ts
</context>

<tasks>

<task type="auto">
  <name>Update Backend Settings</name>
  <files>be/internal/api/settings.go</files>
  <action>
    - Add `"terminal_engine": "wterm"` to `defaultSettings` map.
    - In `UpdateSettings`, add validation for `terminal_engine` to allow only "wterm" or "xterm".
  </action>
  <verify>
    <automated>go test ./be/internal/api/... -v</automated>
  </verify>
  <done>Backend supports and validates the new terminal_engine setting.</done>
</task>

<task type="auto">
  <name>Install Frontend Dependencies</name>
  <files>fe/package.json</files>
  <action>
    - Install xterm.js and its addons: `npm install @xterm/xterm @xterm/addon-fit @xterm/addon-web-links @xterm/addon-webgl` in `fe/` directory.
  </action>
  <verify>
    <automated>cd fe && npm list @xterm/xterm @xterm/addon-fit</automated>
  </verify>
  <done>xterm.js dependencies are present in package.json and node_modules.</done>
</task>

<task type="auto">
  <name>Define Terminal Abstraction and Refactor useSSHSession</name>
  <files>fe/src/features/terminal/types.ts, fe/src/features/terminal/useSSHSession.ts, fe/src/features/settings/hooks/useSettings.ts</files>
  <action>
    - In `fe/src/features/terminal/types.ts`: Export `TerminalHandle` interface:
      ```typescript
      export interface TerminalHandle {
        write: (data: Uint8Array | string) => void;
        focus: () => void;
      }
      ```
    - In `fe/src/features/settings/hooks/useSettings.ts`: 
      - Add `terminal_engine: 'wterm' | 'xterm'` to `AppSettings`.
      - Add `terminal_engine: 'wterm'` to `DEFAULT_SETTINGS`.
    - In `fe/src/features/terminal/useSSHSession.ts`:
      - Remove `import { useTerminal } from '@wterm/react'`.
      - Implement a local `ref = useRef<TerminalHandle | null>(null)`.
      - Implement `write` and `focus` functions that delegate to `ref.current`.
      - Update the return object to use these local implementations.
  </action>
  <verify>
    <automated>cd fe && npx tsc --noEmit</automated>
  </verify>
  <done>useSSHSession no longer depends on @wterm/react and uses the generic TerminalHandle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Settings update payload |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | Settings API | mitigate | Validate terminal_engine value against whitelist (wterm/xterm) in Go backend. |
</threat_model>

<success_criteria>
- Backend allows setting and getting `terminal_engine`.
- Frontend has all necessary packages for xterm.js.
- `useSSHSession` is implementation-agnostic.
</success_criteria>

<output>
After completion, create `.planning/phases/18-terminal-engine-selector/18-01-SUMMARY.md`
</output>
