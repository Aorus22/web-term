---
phase: 18-terminal-engine-selector
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified: [fe/src/features/terminal/TerminalPane.tsx, fe/src/features/settings/components/SettingsPage.tsx]
autonomous: false
requirements: [ENGINE-01, ENGINE-02, ENGINE-03]

must_haves:
  truths:
    - "TerminalPane uses TerminalWrapper to render the terminal"
    - "SettingsPage includes a dropdown to select the terminal engine"
    - "Switching engine in settings immediately updates active terminals"
  artifacts:
    - path: "fe/src/features/terminal/TerminalPane.tsx"
      provides: "Engine-agnostic UI"
    - path: "fe/src/features/settings/components/SettingsPage.tsx"
      provides: "Engine selection UI"
---

<objective>
Integrate the terminal engine abstraction into the main terminal pane and provide a UI in the Settings page for users to switch between engines.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-terminal-engine-selector/18-RESEARCH.md
@fe/src/features/terminal/TerminalPane.tsx
@fe/src/features/settings/components/SettingsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Integrate TerminalWrapper into TerminalPane</name>
  <files>fe/src/features/terminal/TerminalPane.tsx</files>
  <action>
    - Replace the direct import of `Terminal` from `@wterm/react` with `TerminalWrapper` from `./components/TerminalWrapper`.
    - Pass `engine={settings?.terminal_engine || 'wterm'}` to `TerminalWrapper`.
    - Remove `useTerminalMouse` usage from `TerminalPane` (it's now inside `WTermEngine`).
    - Remove `handleTerminalReady` and instead just call `signalReady()` directly if needed, or pass `onReady` to `TerminalWrapper`.
  </action>
  <verify>
    <automated>cd fe && npx tsc --noEmit</automated>
  </verify>
  <done>TerminalPane now uses TerminalWrapper and is engine-agnostic.</done>
</task>

<task type="auto">
  <name>Add Terminal Engine Selector to SettingsPage</name>
  <files>fe/src/features/settings/components/SettingsPage.tsx</files>
  <action>
    - Add a new settings group or item for "Terminal Engine".
    - Implement a select dropdown with options: "wterm (Experimental)" and "xterm.js (Stable)".
    - Bind the value to `settings.terminal_engine` and use `updateSettings` on change.
  </action>
  <verify>
    <automated>cd fe && npx tsc --noEmit</automated>
  </verify>
  <done>SettingsPage allows users to switch terminal engines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Terminal engine switching functionality.</what-built>
  <how-to-verify>
    1. Open the application.
    2. Go to Settings.
    3. Change "Terminal Engine" to "xterm.js".
    4. Open a terminal tab and verify it renders correctly (xterm.js look).
    5. Switch back to "wterm" in settings and verify the terminal updates.
    6. Verify mouse support (clicking, scrolling) works in both engines.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- Users can switch between engines in Settings.
- Both engines work for basic terminal usage (typing, output, resize).
- Selection persists across reloads.
</success_criteria>

<output>
After completion, create `.planning/phases/18-terminal-engine-selector/18-03-SUMMARY.md`
</output>
