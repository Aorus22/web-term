---
phase: 17-terminal-theme-sync
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified: [fe/src/App.tsx, fe/src/index.css, fe/src/features/settings/components/SettingsPage.tsx]
autonomous: false
requirements: [THEME-03, THEME-04]
must_haves:
  truths:
    - "Sidebar background matches the terminal theme's background or secondary color"
    - "Tab bar blends seamlessly with the terminal theme"
    - "Accessibility (contrast) is maintained in light and dark terminal themes"
---

<objective>
Refine the UI adaptation, audit components for style consistency, and ensure accessibility across various terminal themes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-terminal-theme-sync/17-CONTEXT.md
@fe/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and Refine Component Styles</name>
  <files>fe/src/App.tsx, fe/src/index.css</files>
  <action>
    1. Audit the `App.tsx` layout components (Sidebar, Header).
    2. Replace hardcoded `bg-muted/30` or similar with variables that respond better to terminal themes if needed (e.g., using `--sidebar` or `--secondary` variables).
    3. Update `index.css` to remove any redundant terminal theme background overrides if they conflict with the global variables.
    4. Ensure the `.rounded-xl` and other global containers look good with the dynamic colors.
  </action>
  <verify>
    <automated>Check for use of CSS variables in layout components in fe/src/App.tsx</automated>
  </verify>
  <done>Styles refined for consistency</done>
</task>

<task type="auto">
  <name>Task 2: Update Settings Page Preview</name>
  <files>fe/src/features/settings/components/SettingsPage.tsx</files>
  <action>
    Update the theme selection preview in the Settings Page to better reflect how the app will look when a theme is selected. (Optional: ensure the preview matches the actual mapping used in AppThemeProvider).
  </action>
  <verify>
    <automated>Verify that SettingsPage buttons reflect the terminal theme background</automated>
  </verify>
  <done>Settings preview updated</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify multiple themes</name>
  <what-built>Full terminal theme synchronization across the app.</what-built>
  <how-to-verify>
    1. Open Settings.
    2. Switch between 'Nord', 'One Dark', 'Solarized Light', and 'GitHub Light'.
    3. Verify that:
       - The sidebar background changes.
       - The tab bar active/inactive states are readable.
       - Text remains accessible.
       - The terminal itself still looks correct.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- Seamless visual integration between terminal and application UI.
- No "color clashing" between the app and terminal.
- High accessibility standards met for all preset themes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-terminal-theme-sync/17-02-SUMMARY.md`
</output>
