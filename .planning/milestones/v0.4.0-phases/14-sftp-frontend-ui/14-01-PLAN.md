---
phase: 14-sftp-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [fe/package.json, fe/src/components/ui/resizable.tsx, fe/src/lib/api.ts, fe/src/stores/app-store.ts, fe/src/App.tsx, fe/src/features/sftp/components/SFTPView.tsx, fe/src/features/sftp/components/DirectoryBrowser.tsx]
autonomous: true
requirements: [SFTP-01, SFTP-03]
user_setup: []

must_haves:
  truths:
    - "SFTP navigation item exists in sidebar"
    - "Clicking SFTP opens a dual-pane resizable view"
    - "Left and right panes can be independently resized"
  artifacts:
    - path: "fe/src/components/ui/resizable.tsx"
      provides: "Resizable layout components"
    - path: "fe/src/features/sftp/components/SFTPView.tsx"
      provides: "Dual-pane SFTP orchestrator"
    - path: "fe/src/features/sftp/components/DirectoryBrowser.tsx"
      provides: "Reusable pane for file listing"
  key_links:
    - from: "fe/src/App.tsx"
      to: "fe/src/features/sftp/components/SFTPView.tsx"
      via: "sidebarPage state"
---

<objective>
Setup the frontend framework for the SFTP manager, including navigation, API extensions, and the dual-pane resizable layout.

Purpose: Provide the visual shell for file management operations.
Output: Resizable SFTP view accessible from the sidebar.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v0.4.0-phases/14-sftp-frontend-ui/14-RESEARCH.md
@.planning/milestones/v0.4.0-phases/14-sftp-frontend-ui/14-CONTEXT.md
@fe/src/App.tsx
@fe/src/lib/api.ts
@fe/src/stores/app-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Dependencies and UI Components</name>
  <files>fe/package.json, fe/src/components/ui/resizable.tsx</files>
  <action>
    1. Install `react-resizable-panels` in the `fe` directory.
    2. Create `fe/src/components/ui/resizable.tsx` using the standard shadcn/ui template for the Resizable component.
    - Reference: https://ui.shadcn.com/docs/components/resizable
  </action>
  <verify>
    <automated>cd fe && npm list react-resizable-panels && ls src/components/ui/resizable.tsx</automated>
  </verify>
  <done>Resizable components are available for use in the layout.</done>
</task>

<task type="auto">
  <name>Task 2: Extend API Client and App State</name>
  <files>fe/src/lib/api.ts, fe/src/stores/app-store.ts</files>
  <action>
    1. In `fe/src/lib/api.ts`, add `FileInfo` interface matching the backend `FileInfo` struct (name, size, mode, modTime, isDir).
    2. Add `sftpApi` object with `list`, `downloadUrl` (returns string), `upload`, `remove`, `rename`, and `mkdir` methods.
    3. In `fe/src/stores/app-store.ts`, update `sidebarPage` type to include `'sftp'`.
  </action>
  <verify>
    <automated>grep "FileInfo" fe/src/lib/api.ts && grep "'sftp'" fe/src/stores/app-store.ts</automated>
  </verify>
  <done>The application is aware of the SFTP page and can communicate with SFTP backend endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SFTP Navigation and View Skeleton</name>
  <files>fe/src/App.tsx, fe/src/features/sftp/components/SFTPView.tsx, fe/src/features/sftp/components/DirectoryBrowser.tsx</files>
  <action>
    1. Update `fe/src/App.tsx` to add "SFTP" (using `Files` icon from lucide-react) to the sidebar.
    2. Add `SFTPView` to the main content area of `App.tsx`, visible when `sidebarPage === 'sftp'`.
    3. Create `fe/src/features/sftp/components/SFTPView.tsx`:
       - Use `ResizablePanelGroup`, `ResizablePanel`, and `ResizableHandle` to create a horizontal dual-pane layout.
       - Each pane should render a `DirectoryBrowser` component.
    4. Create `fe/src/features/sftp/components/DirectoryBrowser.tsx` as a skeleton component with a "Source Selector" (Select component for choosing between "Local" and existing host connections).
  </action>
  <verify>
    <automated>grep "SFTPView" fe/src/App.tsx && ls fe/src/features/sftp/components/SFTPView.tsx</automated>
  </verify>
  <done>SFTP view is reachable via the sidebar and displays two resizable panes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> SFTP API | API calls with connectionId and path parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `DirectoryBrowser` | mitigate | Ensure path parameters are encoded; backend already cleans paths. |
| T-14-02 | Denial of Service | `sftpApi.list` | mitigate | Implement pagination or virtualization if directory listings are massive (Phase 16). |
</threat_model>

<verification>
- Verify "SFTP" appears in sidebar.
- Clicking "SFTP" clears active session and shows dual-pane layout.
- Verify resizable handle works.
- Verify both panes show a Source Selector.
</verification>

<success_criteria>
- Navigation to /sftp works.
- Dual-pane layout is functional and resizable.
- Source Selector in each pane lists "Local" and available connections.
</success_criteria>

<output>
After completion, create `.planning/phases/14-sftp-frontend-ui/14-01-SUMMARY.md`
</output>
