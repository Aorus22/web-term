---
phase: 14-sftp-frontend-ui
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [fe/src/features/sftp/components/DirectoryBrowser.tsx]
autonomous: true
requirements: [SFTP-01, SFTP-02, SFTP-04]
user_setup: []

must_haves:
  truths:
    - "Selecting a source populates the file list"
    - "Double-clicking a folder navigates into it"
    - "Navigating to '..' goes back to the parent directory"
  artifacts:
    - path: "fe/src/features/sftp/components/DirectoryBrowser.tsx"
      provides: "Functional file listing and navigation"
  key_links:
    - from: "fe/src/features/sftp/components/DirectoryBrowser.tsx"
      to: "fe/src/lib/api.ts"
      via: "sftpApi.list calls"
---

<objective>
Implement the core directory browsing logic, including file listing, double-click navigation, and source selection.

Purpose: Enable users to explore local and remote filesystems.
Output: Functional file browser component used in both panes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v0.4.0-phases/14-sftp-frontend-ui/14-01-PLAN.md
@fe/src/features/sftp/components/DirectoryBrowser.tsx
@fe/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Source Selection and Data Fetching</name>
  <files>fe/src/features/sftp/components/DirectoryBrowser.tsx</files>
  <action>
    1. Update `DirectoryBrowser` to maintain `source` (connectionId) and `path` (defaulting to '.') state.
    2. Use `connectionsApi.list` to populate the "Source Selector" with available connections + "Local".
    3. Use `sftpApi.list` to fetch file entries whenever `source` or `path` changes.
    4. Display a loading indicator while fetching.
  </action>
  <verify>
    <automated>grep "sftpApi.list" fe/src/features/sftp/components/DirectoryBrowser.tsx</automated>
  </verify>
  <done>Files are fetched from the selected source and path.</done>
</task>

<task type="auto">
  <name>Task 2: Implement File List and Navigation</name>
  <files>fe/src/features/sftp/components/DirectoryBrowser.tsx</files>
  <action>
    1. Render the fetched `FileInfo[]` in a table or list.
    2. Add a virtual entry for `..` (parent directory) at the top of the list if the path is not root.
    3. Implement double-click handler on rows:
       - If `isDir`, update `path` to the new directory path.
    4. Add icons based on `isDir` (Folder icon for directories, File icon for files).
  </action>
  <verify>
    <automated>grep "onDoubleClick" fe/src/features/sftp/components/DirectoryBrowser.tsx</automated>
  </verify>
  <done>Users can navigate through the filesystem by double-clicking folders.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Metadata Display and Error Handling</name>
  <files>fe/src/features/sftp/components/DirectoryBrowser.tsx</files>
  <action>
    1. Format file size (e.g., KB, MB) and modification time (relative or formatted date).
    2. Handle API errors (e.g., connection failed, permission denied) by showing a toast or error message in the pane.
    3. Ensure the `DirectoryBrowser` in each pane maintains independent state.
  </action>
  <verify>
    <automated>grep "format" fe/src/features/sftp/components/DirectoryBrowser.tsx</automated>
  </verify>
  <done>The directory browser is robust and provides a clear view of file metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> SFTP API | API calls with connectionId and path parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | `path` state | mitigate | Backend already validates and cleans paths; frontend ensures encoding. |
</threat_model>

<verification>
- Select "Local" in one pane and verify listing.
- Select an SSH connection in the other pane and verify listing.
- Double-click a folder and verify navigation.
- Verify `..` works to go back.
</verification>

<success_criteria>
- Both panes can show different filesystems/paths simultaneously.
- Navigation is fluid and handles errors gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/14-sftp-frontend-ui/14-02-SUMMARY.md`
</output>
