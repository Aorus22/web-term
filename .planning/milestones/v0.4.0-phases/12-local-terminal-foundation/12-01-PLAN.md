---
phase: 12
title: Local Terminal Foundation (Backend)
wave: 1
depends_on: [11]
requirements: [REQ-PTY-INTEGRATION, REQ-LOCAL-WS-HANDLER]
plan: .planning/milestones/v0.4.0-phases/12-local-terminal-foundation/12-01-backend.md
must_haves:
  truths:
    - Backend accepts "local" connection ID via WebSocket.
    - Shell process (PTY) is spawned on the backend machine.
  artifacts:
    - be/internal/ssh/session_manager.go (ManagedSession struct update)
    - be/internal/ssh/proxy.go (PTY spawn logic)
  key_links:
    - WebSocket connect message -> Local PTY spawn.
    - WebSocket binary message -> PTY stdin.
---

# Phase 12-01: Local Terminal Foundation (Backend)

<task id="12-01-01-session-refactor" requirements="REQ-PTY-INTEGRATION, REQ-LOCAL-WS-HANDLER">
<files>
- be/go.mod
- be/internal/ssh/types.go
- be/internal/ssh/session_manager.go
- be/internal/ssh/session_manager_test.go
- be/internal/ssh/proxy.go
</files>
<action>
1. Add `github.com/creack/pty` dependency.
2. Define `SessionType` constants in `types.go`.
3. Refactor `ManagedSession` in `session_manager.go` to make SSH fields optional and add `LocalPTY *os.File` and `Type SessionType`.
4. Update `CreateSession` signature to accommodate optional fields and update all callers in `proxy.go` to pass nil/empty values for now.
5. Update `session_manager_test.go` to verify `ManagedSession` creation and `Type` handling.
</action>
<verify>
<automated>
cd be && go mod tidy && go test ./internal/ssh/session_manager_test.go && go build ./internal/ssh/proxy.go
</automated>
</verify>
<done>
Session management is refactored and build is green.
</done>
</task>

<task id="12-01-02-pty-spawn" requirements="REQ-PTY-INTEGRATION, REQ-LOCAL-WS-HANDLER">
<files>
- be/internal/ssh/proxy.go
</files>
<action>
1. Implement `spawnLocalPTY` helper in `proxy.go`.
2. Update `HandleWebSocket` to use it when `ConnectionID == "local"`.
3. Refactor `masterForwarder` and `wsMessageLoop` to handle the single PTY file descriptor for local sessions (reading/writing to the same `*os.File`).
</action>
<verify>
<automated>
cd be && go build ./...
</automated>
</verify>
<done>
Backend successfully spawns and forwards I/O for local PTYs.
</done>
</task>

<task id="12-01-03-pty-resize" requirements="REQ-PTY-INTEGRATION, REQ-LOCAL-WS-HANDLER">
<files>
- be/internal/ssh/proxy.go
</files>
<action>
Implement terminal resize logic for local PTYs in `wsMessageLoop` using `pty.Setsize`.
</action>
<verify>
<automated>
cd be && go build ./...
</automated>
</verify>
<done>
Local PTY resizing is implemented and compilation succeeds.
</done>
</task>
