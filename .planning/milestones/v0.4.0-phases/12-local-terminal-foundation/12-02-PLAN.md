---
phase: 12
title: Local Terminal Foundation (Frontend)
wave: 1
depends_on: [11]
requirements: [REQ-LOCAL-UI-OPTION]
plan: .planning/milestones/v0.4.0-phases/12-local-terminal-foundation/12-02-frontend.md
must_haves:
  truths:
    - "Local Terminal" card is visible in New Tab view.
    - Clicking it opens a terminal tab with "Local" label.
  artifacts:
    - fe/src/components/NewTabView.tsx ("Local Terminal" button)
    - fe/src/features/terminal/types.ts (SSHSession updates)
  key_links:
    - NewTabView click -> app-store session add -> Terminal connection.
---

# Phase 12-02: Local Terminal Foundation (Frontend)

<task id="12-02-01-types" requirements="REQ-LOCAL-UI-OPTION">
<files>
- fe/src/features/terminal/types.ts
</files>
<action>
Update `SSHSession` and `ConnectOptions` to allow optional SSH-specific fields and add a `type` field.
</action>
<verify>
<automated>
cd fe && npm run typecheck
</automated>
</verify>
<done>
TypeScript types are compatible with local sessions.
</done>
</task>

<task id="12-02-02-ui-newtab" requirements="REQ-LOCAL-UI-OPTION">
<files>
- fe/src/components/NewTabView.tsx
- fe/src/stores/app-store.ts
</files>
<action>
1. Add "Local Terminal" card to `NewTabView.tsx`.
2. Implement `handleLocalConnect` to add a session with `connectionId: "local"`.
</action>
<verify>
<automated>
cd fe && npm run lint
</automated>
</verify>
<done>
UI allows selecting local terminal.
</done>
</task>

<task id="12-02-03-ui-polish" requirements="REQ-LOCAL-UI-OPTION">
<files>
- fe/src/features/terminal/components/TerminalPane.tsx
</files>
<action>
Adjust status messages and labels in `TerminalPane` to reflect local session status correctly.
</action>
<verify>
<automated>
cd fe && npm run lint
</automated>
</verify>
<done>
Terminal UI is polished for local sessions.
</done>
</task>
