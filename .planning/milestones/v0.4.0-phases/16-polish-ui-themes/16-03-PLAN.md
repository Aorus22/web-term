---
phase: 16-polish-ui-themes
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [fe/src/features/sftp/stores/sftp-store.ts, fe/src/hooks/use-sftp-transfer.ts, fe/src/features/sftp/components/TransferManager.tsx, fe/src/features/sftp/components/SFTPView.tsx]
autonomous: true
requirements: [SFTP-04]
must_haves:
  truths:
    - "Active transfers are visible in a dedicated panel"
    - "Transfer progress updates in real-time in the panel"
  artifacts:
    - path: "fe/src/features/sftp/stores/sftp-store.ts"
      provides: "Global SFTP transfer state"
    - path: "fe/src/features/sftp/components/TransferManager.tsx"
      provides: "UI for managing active/completed transfers"
---

<objective>
Refine progress indicators by implementing a dedicated Transfer Manager that tracks all background operations (uploads, downloads, inter-host transfers).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-polish-ui-themes/16-CONTEXT.md
@fe/src/hooks/use-sftp-transfer.ts
@fe/src/features/sftp/components/SFTPView.tsx
</context>

<tasks>

<task type="auto">
  <name>Create sftp-store and refactor useSFTPTransfer</name>
  <files>fe/src/features/sftp/stores/sftp-store.ts, fe/src/hooks/use-sftp-transfer.ts</files>
  <action>
    - Create `sftp-store.ts` using `zustand`. It should store a list of `transfers: TransferStatus[]`.
    - `TransferStatus` should include: `id`, `fileName`, `status`, `progress`, `type`, `error`.
    - Add actions to `addTransfer`, `updateTransfer`, `clearCompleted`.
    - Refactor `useSFTPTransfer.ts`:
      - Instead of just creating local `EventSource` and toasts, it should `addTransfer` to the store when a transfer starts.
      - Update the store on each `onmessage` event.
      - Keep existing toast logic as a supplementary "heads-up" notification if desired, or move entirely to the Transfer Manager. (Decision: Keep toasts for start/finish, use panel for detail).
  </action>
  <verify>
    - `sftp-store.ts` correctly manages transfer state.
    - `useSFTPTransfer.ts` updates the store with SSE data.
  </verify>
  <done>Transfer state management centralized.</done>
</task>

<task type="auto">
  <name>Implement TransferManager UI</name>
  <files>fe/src/features/sftp/components/TransferManager.tsx</files>
  <action>
    - Create a `TransferManager` component that displays the list of transfers from the store.
    - Each transfer item should show: `fileName`, `type` (icon), `status`, and a `Progress` bar (using `@/components/ui/progress`).
    - Add a "Clear Completed" button.
    - Style it as a collapsible bottom panel or a scrollable popover. (Decision: Bottom panel in `SFTPView`).
  </action>
  <verify>
    - Component renders active transfers with progress bars.
    - "Clear Completed" removes finished/failed transfers from the list.
  </verify>
  <done>Transfer Manager UI created.</done>
</task>

<task type="auto">
  <name>Integrate TransferManager into SFTPView</name>
  <files>fe/src/features/sftp/components/SFTPView.tsx</files>
  <action>
    - Import `TransferManager`.
    - Update `SFTPView.tsx` to include `TransferManager` at the bottom of the layout, below the `ResizablePanelGroup`.
    - Ensure it doesn't take up too much space (collapsible or fixed height with scroll).
  </action>
  <verify>
    - SFTP view now includes the Transfer Manager.
    - Starting a file operation (upload/drag-drop) immediately shows an entry in the manager.
  </verify>
  <done>SFTP interface finalized with progress tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SSE → UI | Progress data flows from backend via SSE |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Information Disclosure | Progress | accept | Progress data only includes filenames and byte counts for the user's own active transfers. |
</threat_model>

<success_criteria>
- Multi-transfer tracking is functional.
- Real-time progress bars update as files are moved.
</success_criteria>

<output>
After completion, create `.planning/phases/16-polish-ui-themes/16-03-SUMMARY.md`
</output>
