---
phase: 16-polish-ui-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [fe/src/features/sftp/components/SftpBreadcrumbs.tsx, fe/src/features/sftp/components/FileIcon.tsx, fe/src/features/sftp/components/DirectoryBrowser.tsx]
autonomous: true
requirements: [SFTP-01, SFTP-04]
must_haves:
  truths:
    - "User can navigate by clicking breadcrumb segments"
    - "Files display specific icons based on their type/extension"
  artifacts:
    - path: "fe/src/features/sftp/components/SftpBreadcrumbs.tsx"
      provides: "Interactive path navigation"
    - path: "fe/src/features/sftp/components/FileIcon.tsx"
      provides: "Dynamic file type icons"
---

<objective>
Improve SFTP navigation and visual identity by implementing interactive breadcrumbs and file-type-specific icons.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-polish-ui-themes/16-CONTEXT.md
@fe/src/features/sftp/components/DirectoryBrowser.tsx
</context>

<tasks>

<task type="auto">
  <name>Create SftpBreadcrumbs and FileIcon components</name>
  <files>fe/src/features/sftp/components/SftpBreadcrumbs.tsx, fe/src/features/sftp/components/FileIcon.tsx</files>
  <action>
    - Create `FileIcon.tsx`: A component that takes `name` and `isDir` and returns the appropriate `lucide-react` icon. Support `Folder`, `FileImage` (.jpg, .png, .svg), `FileArchive` (.zip, .tar, .gz), `FileCode` (.js, .ts, .go, .py, .json), `FileText` (.txt, .md, .log), and default `File`.
    - Create `SftpBreadcrumbs.tsx`: A component that takes `path: string` and `onNavigate: (path: string) => void`. It should split the path by `/`, filter out empty strings, and render clickable segments separated by `/`. Clicking a segment should construct the full path up to that segment and call `onNavigate`.
    - Handle both absolute (`/etc/ssh`) and relative (`.`, `path/to/dir`) paths.
  </action>
  <verify>
    - Files exist and export the components.
    - `FileIcon` returns different icons for different extensions.
    - `SftpBreadcrumbs` correctly parses `/home/user/docs` into 3 clickable segments.
  </verify>
  <done>Components created and ready for integration.</done>
</task>

<task type="auto">
  <name>Integrate Breadcrumbs and Icons into DirectoryBrowser</name>
  <files>fe/src/features/sftp/components/DirectoryBrowser.tsx</files>
  <action>
    - Import `SftpBreadcrumbs` and `FileIcon`.
    - Replace the static path display (`<div className="flex-1 px-2 ... truncate">`) with the `SftpBreadcrumbs` component.
    - In the file list table, replace the hardcoded `Folder` and `FileIcon` logic with the new `FileIcon` component, passing the file name and `isDir` property.
    - Ensure `onNavigate` in breadcrumbs updates the `path` state and clears selection.
    - (Polish) Add a subtle transition or animation to the breadcrumb segments on hover.
  </action>
  <verify>
    - SFTP pane shows interactive breadcrumbs.
    - Clicking a breadcrumb segment navigates to that folder.
    - Files show appropriate icons (e.g. image icon for .png).
  </verify>
  <done>Navigation and visual components integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Navigation | Path manipulation occurs locally in frontend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | Breadcrumbs | mitigate | Path constructed from segments is sent to existing backend API which validates existence/permissions |
</threat_model>

<success_criteria>
- Navigation via breadcrumbs works for both Local and Remote sources.
- Visual variety in file icons based on extension.
</success_criteria>

<output>
After completion, create `.planning/phases/16-polish-ui-themes/16-01-SUMMARY.md`
</output>
