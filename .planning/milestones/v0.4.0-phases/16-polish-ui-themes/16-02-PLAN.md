---
phase: 16-polish-ui-themes
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [fe/src/features/sftp/hooks/use-sftp-shortcuts.ts, fe/src/features/sftp/components/DirectoryBrowser.tsx]
autonomous: true
requirements: [SFTP-04]
must_haves:
  truths:
    - "Pressing Backspace goes to parent directory"
    - "Pressing Enter on a folder enters it"
    - "Pressing Delete triggers delete confirmation"
  artifacts:
    - path: "fe/src/features/sftp/hooks/use-sftp-shortcuts.ts"
      provides: "Scoped SFTP keyboard handlers"
---

<objective>
Implement keyboard shortcuts for common SFTP operations to provide a more efficient "power user" experience.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-polish-ui-themes/16-CONTEXT.md
@fe/src/features/sftp/components/DirectoryBrowser.tsx
@fe/src/hooks/use-keyboard-shortcuts.ts
</context>

<tasks>

<task type="auto">
  <name>Create useSFTPShortcuts hook</name>
  <files>fe/src/features/sftp/hooks/use-sftp-shortcuts.ts</files>
  <action>
    - Create a hook that accepts handlers for: `onEnter`, `onBackspace`, `onDelete`, `onRefresh`, `onCopy`, `onCut`, `onPaste`.
    - Use `useEffect` to listen for `keydown` events.
    - **CRITICAL:** Ensure shortcuts only trigger if the relevant `DirectoryBrowser` pane is focused (check `ref.current.contains(document.activeElement)`).
    - Handle `Enter`, `Backspace`, `Delete`.
    - Handle `F5` and `Ctrl+R` for refresh.
    - Handle `Ctrl+C`, `Ctrl+X`, `Ctrl+V` for clipboard actions.
  </action>
  <verify>
    - Hook exists and implements the event listeners.
    - Focus check logic is present.
  </verify>
  <done>Hook created and ready for use.</done>
</task>

<task type="auto">
  <name>Integrate shortcuts into DirectoryBrowser</name>
  <files>fe/src/features/sftp/components/DirectoryBrowser.tsx</files>
  <action>
    - Add a `ref` to the root `div` of `DirectoryBrowser`.
    - Add `tabIndex={0}` to the root `div` to make it focusable.
    - Add `onFocus` handler to keep track of "active" pane if needed, or just rely on `document.activeElement`.
    - Call `useSFTPShortcuts` with appropriate handlers:
      - `onEnter`: If folder selected → navigate; if file selected → download.
      - `onBackspace`: Navigate to `..`.
      - `onDelete`: Trigger `setIsDeleteDialogOpen(true)`.
      - `onRefresh`: Call `refreshDir()`.
      - `onCopy`/`onCut`: Call `handleAction`.
      - `onPaste`: Call `handlePaste`.
    - Add visual feedback (e.g., a subtle border or glow) when the pane is focused.
  </action>
  <verify>
    - Keyboard shortcuts work as expected when pane is focused.
    - Shortcuts do NOT trigger when pane is not focused or when typing in a dialog input.
  </verify>
  <done>Shortcuts integrated and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Actions | Shortcuts trigger existing UI handlers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Elevation of Privilege | Shortcuts | accept | Shortcuts only trigger actions already available in the UI. Standard backend authorization applies. |
</threat_model>

<success_criteria>
- Keyboard navigation (Backspace, Enter) is functional.
- Standard file operation shortcuts (Delete, Ctrl+C/V) are functional.
</success_criteria>

<output>
After completion, create `.planning/phases/16-polish-ui-themes/16-02-SUMMARY.md`
</output>
