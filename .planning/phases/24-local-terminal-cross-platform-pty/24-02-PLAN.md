---
phase: 24-local-terminal-cross-platform-pty
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - desktop-gpui/crates/webterm/src/views/new_tab_modal.rs
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/src/session.rs
  - desktop-gpui/crates/webterm/tests/local_tab_test.rs
files_deleted: []
autonomous: true
requirements: [TERM-05]
user_setup: []
must_haves:
  truths:
    - "New Tab modal presents Local Shell as the primary first-class action item at the top of the modal"
    - "AppState::open_local_tab supports cwd preservation from the active host/workspace environment"
    - "Local terminal tab title clearly identifies local shell session"
    - "Process exit and termination transitions tab status to disconnected with restart and close options"
  artifacts:
    - path: "desktop-gpui/crates/webterm/src/views/new_tab_modal.rs"
      provides: "New Tab modal with Local Shell prioritized as first-class option"
      min_lines: 80
    - path: "desktop-gpui/crates/webterm/src/app_state.rs"
      provides: "open_local_tab and open_local_tab_with_cwd implementations"
      min_lines: 200
  key_links:
    - from: "desktop-gpui/crates/webterm/src/views/new_tab_modal.rs"
      to: "desktop-gpui/crates/webterm/src/app_state.rs"
      via: "Clicking Local Shell card triggers open_local_tab"
      pattern: "open_local_tab"
---

<objective>
Enhance the desktop GPUI client to ensure Local Terminal is a first-class New Tab option with cwd awareness, responsive lifecycle handling, and intuitive exit indicators.

Purpose: Provide full desktop interaction parity for local shell sessions (`TERM-05`).
Output: Updated `new_tab_modal.rs`, `app_state.rs`, `session.rs`, and extended test coverage in `local_tab_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-local-terminal-cross-platform-pty/24-CONTEXT.md
@.planning/phases/24-local-terminal-cross-platform-pty/24-RESEARCH.md
@.planning/phases/24-local-terminal-cross-platform-pty/CONPTY-DECISION.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Prioritize Local Shell in New Tab Modal and add open_local_tab_with_cwd</name>
<files>desktop-gpui/crates/webterm/src/views/new_tab_modal.rs, desktop-gpui/crates/webterm/src/app_state.rs</files>
<read_first>desktop-gpui/crates/webterm/src/views/new_tab_modal.rs, desktop-gpui/crates/webterm/src/app_state.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/views/new_tab_modal.rs`:
   - Verify and refine the New Tab modal card ordering: "💻 Local Shell" must be the prominent top option with a clear badge/description ("Local Host Shell (ConPTY / POSIX PTY)").
   - Add shortcut hint (`Enter` or single click to launch local shell).
2. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Add `pub fn open_local_tab_with_cwd(&mut self, cwd: Option<String>, cx: &mut Context<Self>)`:
     - Allocates a tab ID with title `"Local Shell"`.
     - Uses `WsConnectRequest::for_local_with_cwd(80, 24, cwd)` to initiate WebSocket streaming to backend.
     - Attaches the handle to `session_manager`.
   - Update `open_local_tab(&mut self, cx: &mut Context<Self>)` to delegate to `open_local_tab_with_cwd(None, cx)`.
   - Ensure local sessions are tagged with `session_type = "local"` and `connection_id = Some("local")`.
</action>
<verify>
cargo check --workspace --manifest-path desktop-gpui/Cargo.toml
</verify>
<done>
New Tab modal and AppState local tab methods updated and type-checked cleanly.
</done>
</task>

<task type="auto">
<name>Task 2: Handle local process exit and write unit tests for tab lifecycle</name>
<files>desktop-gpui/crates/webterm/src/session.rs, desktop-gpui/crates/webterm/tests/local_tab_test.rs</files>
<read_first>desktop-gpui/crates/webterm/src/session.rs, desktop-gpui/crates/webterm/tests/local_tab_test.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/session.rs`:
   - Ensure `TerminalTab` methods correctly expose whether a session is local (`is_local() -> bool`).
   - For local tabs, when status transitions to `Disconnected`, provide helper to determine restartability.
2. In `desktop-gpui/crates/webterm/tests/local_tab_test.rs`:
   - Add test `test_local_tab_is_local_flag_and_restartability` verifying `is_local()` returns true for local tabs and false for SSH tabs.
   - Add test `test_local_tab_with_custom_cwd` testing request generation and session creation with cwd parameter.
3. Run test suite to verify all tests pass.
</action>
<verify>
cargo test --workspace --manifest-path desktop-gpui/Cargo.toml
cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings
</verify>
<done>
All tests pass and clippy produces zero warnings.
</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace --manifest-path desktop-gpui/Cargo.toml` to verify all tests pass.
Run `cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings` to verify zero clippy issues.
</verification>
