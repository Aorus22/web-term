---
phase: 24-local-terminal-cross-platform-pty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop-gpui/crates/backend-client/src/terminal_ws.rs
  - desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs
  - desktop-gpui/crates/webterm/tests/local_tab_test.rs
files_deleted: []
autonomous: true
requirements: [TERM-05]
user_setup: []
must_haves:
  truths:
    - "ConPTY architecture decision is formally recorded and validated against the backend PTY bridge"
    - "WsConnectRequest supports local shell requests with optional custom/inherited working directory"
    - "Contract tests verify local shell connection request serialization with and without custom cwd"
    - "Mock WebSocket integration test exercises local shell stream, resize, and clean termination"
  artifacts:
    - path: ".planning/phases/24-local-terminal-cross-platform-pty/CONPTY-DECISION.md"
      provides: "Architectural Decision Record documenting Go backend ConPTY vs Rust portable-pty"
      min_lines: 40
    - path: "desktop-gpui/crates/backend-client/src/terminal_ws.rs"
      provides: "WsConnectRequest::for_local_with_cwd constructor for local shell PTY"
      min_lines: 100
  key_links:
    - from: "desktop-gpui/crates/backend-client/src/terminal_ws.rs"
      to: "be/internal/ssh/spawn_windows.go"
      via: "WebSocket connect payload with cwd and dimensions matching ConPtyOption"
      pattern: "for_local_with_cwd"
---

<objective>
Formalize the ConPTY architecture decision record and extend `WsConnectRequest` in `webterm-backend-client` to support local terminal instantiation with optional working directory (CWD), validated by contract and mock stream tests.

Purpose: Provide robust protocol primitives for local shell sessions across Windows (ConPTY) and Linux (POSIX PTY) with full working directory fidelity.
Output: Extended `terminal_ws.rs`, updated contract tests in `terminal_ws_test.rs` and `local_tab_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-local-terminal-cross-platform-pty/24-CONTEXT.md
@.planning/phases/24-local-terminal-cross-platform-pty/24-RESEARCH.md
@.planning/phases/24-local-terminal-cross-platform-pty/CONPTY-DECISION.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Add WsConnectRequest::for_local_with_cwd and enhance local request constructors</name>
<files>desktop-gpui/crates/backend-client/src/terminal_ws.rs</files>
<read_first>desktop-gpui/crates/backend-client/src/terminal_ws.rs, be/internal/ssh/spawn_windows.go, be/internal/ssh/spawn_unix.go</read_first>
<action>
1. In `desktop-gpui/crates/backend-client/src/terminal_ws.rs`:
   - Add `pub fn for_local_with_cwd(cols: u16, rows: u16, cwd: Option<String>) -> Self` which initializes `WsConnectRequest` with `session_type: Some("local".to_string())`, `connection_id: Some("local".to_string())`, `cwd`, and standard dimensions and term (`xterm-256color`).
   - Update `pub fn for_local(cols: u16, rows: u16) -> Self` to delegate to `Self::for_local_with_cwd(cols, rows, None)`.
2. Ensure docs on `for_local_with_cwd` explain how it resolves in the backend: on Windows via `conpty.ConPtyWorkDir`, on Linux via `c.Dir`, with fallback to user home directory if `cwd` is omitted or empty.
</action>
<verify>
cargo test --manifest-path desktop-gpui/crates/backend-client/Cargo.toml
</verify>
<done>
`WsConnectRequest::for_local_with_cwd` is implemented, documented, and compiling cleanly.
</done>
</task>

<task type="auto">
<name>Task 2: Expand unit and mock WebSocket contract tests for local shell requests</name>
<files>desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs, desktop-gpui/crates/webterm/tests/local_tab_test.rs</files>
<read_first>desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs, desktop-gpui/crates/webterm/tests/local_tab_test.rs</read_first>
<action>
1. In `desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs`:
   - Add unit tests asserting serialization of `WsConnectRequest::for_local_with_cwd(120, 40, Some("/home/user/project".to_string()))` and `None`.
   - Verify that when `cwd` is `Some`, `"cwd": "/home/user/project"` is serialized in the JSON payload, and when `None`, the `"cwd"` key is omitted due to `skip_serializing_if = "Option::is_none"`.
2. In `desktop-gpui/crates/webterm/tests/local_tab_test.rs`:
   - Add a test verifying `for_local_with_cwd` in the mock WebSocket handshake where the mock server verifies the received `cwd` field.
3. Run workspace tests and ensure all tests pass.
</action>
<verify>
cargo test --workspace --manifest-path desktop-gpui/Cargo.toml
cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings
</verify>
<done>
All tests pass and clippy produces zero warnings.
</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace --manifest-path desktop-gpui/Cargo.toml` to verify all tests pass across all crates.
Run `cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings` to verify code quality.
</verification>
