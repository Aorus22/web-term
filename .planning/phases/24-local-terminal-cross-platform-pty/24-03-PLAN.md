---
phase: 24-local-terminal-cross-platform-pty
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - desktop-gpui/crates/webterm/tests/local_smoke_test.rs
  - be/internal/ssh/spawn_test.go
  - .github/workflows/desktop-ci.yml
files_deleted: []
autonomous: true
requirements: [TERM-05]
user_setup: []
must_haves:
  truths:
    - "A Windows & Linux automated smoke test exercises real local PTY spawning, terminal I/O, and resize"
    - "Go backend unit test exercises spawnLocalPTY and resizeLocalPTY directly on the host OS"
    - "GitHub Actions desktop-ci.yml runs workspace tests on both windows-latest and ubuntu-latest with TEST_BACKEND_PATH set"
    - "100% of workspace tests pass and clippy produces zero warnings"
  artifacts:
    - path: "desktop-gpui/crates/webterm/tests/local_smoke_test.rs"
      provides: "End-to-end local terminal smoke test using real backend supervisor"
      min_lines: 70
    - path: "be/internal/ssh/spawn_test.go"
      provides: "Go unit tests for spawnLocalPTY and resizeLocalPTY"
      min_lines: 40
  key_links:
    - from: "desktop-gpui/crates/webterm/tests/local_smoke_test.rs"
      to: "desktop-gpui/test-support/backend.exe"
      via: "TEST_BACKEND_PATH supervisor execution"
      pattern: "TEST_BACKEND_PATH"
---

<objective>
Implement end-to-end automated smoke tests for the local terminal path on Windows (ConPTY) and Linux (POSIX PTY), along with Go backend spawn tests and CI verification in GitHub Actions.

Purpose: Provide permanent automated regression coverage verifying that local terminals spawn, stream, resize, and shut down cleanly on Windows and Linux (`TERM-05`).
Output: New `local_smoke_test.rs`, `spawn_test.go`, and verified `desktop-ci.yml`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-local-terminal-cross-platform-pty/24-CONTEXT.md
@.planning/phases/24-local-terminal-cross-platform-pty/24-RESEARCH.md
@.planning/phases/24-local-terminal-cross-platform-pty/CONPTY-DECISION.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Implement Go backend spawn and resize tests</name>
<files>be/internal/ssh/spawn_test.go</files>
<read_first>be/internal/ssh/spawn_windows.go, be/internal/ssh/spawn_unix.go</read_first>
<action>
1. In `be/internal/ssh/spawn_test.go`:
   - Create tests for `spawnLocalPTY` and `resizeLocalPTY`.
   - Test 1: `TestSpawnLocalPTY_Default`:
     - Spawns local PTY with `Cols: 80, Rows: 24`.
     - Verifies `pty != nil`, `pid > 0`, and `err == nil`.
     - Writes a newline or simple command.
     - Resizes PTY to 100x30 via `resizeLocalPTY(pty, 100, 30)` and asserts no error.
     - Closes PTY and ensures clean shutdown.
   - Test 2: `TestSpawnLocalPTY_WithCwd`:
     - Spawns local PTY with a specific working directory (e.g. `t.TempDir()`).
     - Verifies successful spawn and closes PTY.
2. Run `go test ./internal/ssh/...` from `be/` to verify tests pass.
</action>
<verify>
cd be && go test -v -run TestSpawnLocalPTY ./internal/ssh
</verify>
<done>
Go backend local PTY spawn and resize unit tests pass cleanly.
</done>
</task>

<task type="auto">
<name>Task 2: Implement Rust end-to-end local terminal smoke test</name>
<files>desktop-gpui/crates/webterm/tests/local_smoke_test.rs, .github/workflows/desktop-ci.yml</files>
<read_first>desktop-gpui/crates/supervisor/tests/integration.rs, desktop-gpui/crates/backend-client/src/terminal_ws.rs, .github/workflows/desktop-ci.yml</read_first>
<action>
1. In `desktop-gpui/crates/webterm/tests/local_smoke_test.rs`:
   - Locate test backend fixture via `TEST_BACKEND_PATH` or default `desktop-gpui/test-support/backend.exe` / `backend`. If absent, gracefully skip with an informative message.
   - Use `webterm_supervisor::Supervisor` to start backend on a loopback address with temp SQLite DB and encryption key.
   - Connect via `webterm_backend_client::TerminalWsClient::connect` using `WsConnectRequest::for_local(80, 24)`.
   - Verify connection succeeds and returns a valid session ID.
   - Send input (e.g. newline or `echo PTY_SMOKE_OK`).
   - Read from output receiver within a reasonable timeout and verify data is received from the shell.
   - Send resize command (`handle.resize(100, 30)`).
   - Cleanly disconnect and stop the supervisor.
2. Verify `.github/workflows/desktop-ci.yml`:
   - Confirm `TEST_BACKEND_PATH` is passed on both `ubuntu-latest` and `windows-latest` jobs.
3. Run `cargo test --test local_smoke_test --manifest-path desktop-gpui/crates/webterm/Cargo.toml`.
</action>
<verify>
cargo test --workspace --manifest-path desktop-gpui/Cargo.toml
cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings
</verify>
<done>
Rust end-to-end smoke test passes and all CI requirements are satisfied.
</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace --manifest-path desktop-gpui/Cargo.toml` to verify all 85+ tests pass across all crates.
Run `cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings` to verify zero warnings.
Run Go tests: `cd be && go test ./internal/ssh/...`
</verification>
