---
phase: 21-terminal-rendering-alacritty-engine
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - desktop-gpui/crates/terminal/src/lib.rs
  - desktop-gpui/crates/terminal/src/event.rs
  - desktop-gpui/crates/terminal/src/terminal.rs
  - desktop-gpui/crates/terminal/tests/terminal_state_test.rs
files_deleted: []
autonomous: true
requirements: [TERM-01, TERM-03, TERM-04]
user_setup: []
must_haves:
  truths:
    - "Terminal state machine processes raw ANSI escape sequences into Grid cells with correct colors, bold/italic flags, and character positions"
    - "Alternate screen switching (smcup / rmcup for vim/htop) works correctly without losing primary screen history"
    - "Scrollback lines accumulate when output exceeds terminal height, and scroll_display shifts the visible viewport"
    - "Resize updates cols and rows and notifies listeners with new dimensions"
  artifacts:
    - path: "desktop-gpui/crates/terminal/src/terminal.rs"
      provides: "Terminal state engine wrapping alacritty_terminal::Term with parser and I/O worker"
      min_lines: 100
      exports: ["Terminal", "TerminalConfig", "TerminalSize"]
    - path: "desktop-gpui/crates/terminal/src/event.rs"
      provides: "Event handling proxy forwarding terminal redraw and wakeup notifications"
      min_lines: 40
    - path: "desktop-gpui/crates/terminal/tests/terminal_state_test.rs"
      provides: "Automated test suite verifying VTE parsing, colors, alternate screen, and scrollback"
      min_lines: 60
  key_links:
    - from: "desktop-gpui/crates/terminal/src/terminal.rs"
      to: "desktop-gpui/crates/terminal/src/event.rs"
      via: "event proxy injected into alacritty_terminal::Term"
      pattern: "EventProxy"
---

<objective>
Implement the terminal state layer and event loop inside webterm-terminal using alacritty_terminal 0.25.1.

Purpose: Provide a robust, VTE-compliant terminal state machine that processes incoming byte streams, manages scrollback, handles alternate screen transitions (vim/htop), and notifies GPUI when repaints are needed.
Output: terminal.rs, event.rs, and passing test suite in terminal_state_test.rs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-terminal-rendering-alacritty-engine/21-CONTEXT.md
@.planning/phases/21-terminal-rendering-alacritty-engine/21-RESEARCH.md
@.planning/phases/21-terminal-rendering-alacritty-engine/21-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement event proxy and terminal state machine</name>
  <files>desktop-gpui/crates/terminal/src/event.rs, desktop-gpui/crates/terminal/src/terminal.rs, desktop-gpui/crates/terminal/src/lib.rs</files>
  <read_first>desktop-gpui/crates/terminal/src/colors.rs</read_first>
  <action>
1. Create desktop-gpui/crates/terminal/src/event.rs:
   - Implement GpuiEventProxy implementing alacritty_terminal::event::EventListener.
   - Handle resize, title changes, bell, wakeups, and clipboard notifications.
   - Use flume channels or callbacks to signal the UI thread when terminal contents change.

2. Create desktop-gpui/crates/terminal/src/terminal.rs:
   - Define TerminalSize { cols: usize, rows: usize }.
   - Define TerminalConfig { font_size, line_height, scrollback_limit: usize, ... }.
   - Implement Terminal struct holding Arc<parking_lot::Mutex<Term<GpuiEventProxy>>> and Processor.
   - Implement process_bytes(&mut self, bytes: &[u8]): parse VTE input into terminal grid.
   - Implement resize(&mut self, cols: usize, rows: usize): update terminal grid dimensions.
   - Implement scroll_display(&mut self, delta: i32): scroll viewport up/down in history.
   - Implement selection methods: start_selection, update_selection, clear_selection, selection_text.

3. Export Terminal, TerminalConfig, TerminalSize, and GpuiEventProxy in src/lib.rs.
  </action>
  <verify>
    <automated>cargo check -p webterm-terminal --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - terminal.rs compiles and exposes state methods
    - EventProxy implements alacritty_terminal::event::EventListener
  </acceptance_criteria>
  <done>Terminal state engine implemented</done>
</task>

<task type="auto">
  <name>Task 2: Automated test suite for terminal state, alternate screen & scrollback</name>
  <files>desktop-gpui/crates/terminal/tests/terminal_state_test.rs</files>
  <read_first>desktop-gpui/crates/terminal/src/terminal.rs</read_first>
  <action>
Create tests/terminal_state_test.rs covering:
1. ANSI text & color parsing: Feed "\x1b[31mRed\x1b[0m \x1b[1mBold\x1b[0m" and verify cell character and style flags.
2. TrueColor 24-bit RGB: Feed "\x1b[38;2;255;100;50mRGB\x1b[0m" and verify RGB color value.
3. Alternate screen switching: Feed "\x1b[?1049h" (enter alternate screen), write text, feed "\x1b[?1049l" (leave alternate screen), verify primary screen contents restored.
4. Scrollback accumulation: Feed lines exceeding terminal row capacity, verify scrollback lines accumulated and viewport scrolling shifts display lines.
5. Grid resize: Resize terminal from 80x24 to 120x40 and verify new dimensions.
  </action>
  <verify>
    <automated>cargo test -p webterm-terminal --test terminal_state_test --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - All 5 test cases pass cleanly
  </acceptance_criteria>
  <done>Terminal state tests green</done>
</task>

</tasks>
