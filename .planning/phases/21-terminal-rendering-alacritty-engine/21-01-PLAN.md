---
phase: 21-terminal-rendering-alacritty-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop-gpui/Cargo.toml
  - desktop-gpui/crates/terminal/Cargo.toml
  - desktop-gpui/crates/terminal/src/lib.rs
  - desktop-gpui/crates/terminal/src/colors.rs
  - .planning/phases/21-terminal-rendering-alacritty-engine/SPIKE-gpui-terminal.md
files_deleted: []
autonomous: true
requirements: [TERM-01]
user_setup: []
must_haves:
  truths:
    - "Spike report SPIKE-gpui-terminal.md records an explicit go/no-go verdict on gpui-terminal citing the GPUI version conflict"
    - "Workspace Cargo.toml includes crates/terminal with alacritty_terminal = '0.25.1' and exact pins"
    - "ColorPalette converts ANSI, 256-color, and 24-bit TrueColor to GPUI Rgba colors"
  artifacts:
    - path: ".planning/phases/21-terminal-rendering-alacritty-engine/SPIKE-gpui-terminal.md"
      provides: "Formal spike report with dependency audit and architecture decision"
      contains: "NO-GO"
    - path: "desktop-gpui/crates/terminal/Cargo.toml"
      provides: "webterm-terminal crate manifest"
      contains: "alacritty_terminal"
    - path: "desktop-gpui/crates/terminal/src/colors.rs"
      provides: "ColorPalette mapping 16 ANSI + 256 color + RGB TrueColor to GPUI colors"
      min_lines: 50
  key_links:
    - from: "desktop-gpui/crates/terminal/Cargo.toml"
      to: "desktop-gpui/Cargo.toml"
      via: "workspace member crates/terminal"
      pattern: "crates/terminal"
---

<objective>
Deliver the spike report on gpui-terminal (Criterion 5), add alacritty_terminal to workspace dependencies, and scaffold the webterm-terminal crate with color palette support.

Purpose: Resolve the gpui-terminal decision explicitly and establish the terminal crate boundary for subsequent rendering and state tasks.
Output: SPIKE-gpui-terminal.md, desktop-gpui/crates/terminal crate scaffold with colors.rs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-terminal-rendering-alacritty-engine/21-CONTEXT.md
@.planning/phases/21-terminal-rendering-alacritty-engine/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record spike report SPIKE-gpui-terminal.md</name>
  <files>.planning/phases/21-terminal-rendering-alacritty-engine/SPIKE-gpui-terminal.md</files>
  <read_first>.planning/phases/21-terminal-rendering-alacritty-engine/21-RESEARCH.md</read_first>
  <action>
Create SPIKE-gpui-terminal.md recording the formal evaluation of gpui-terminal 0.1.0:
- Audit of dependencies: gpui-terminal pins gpui = "0.2.2", whereas desktop-gpui is pinned to gpui-pre = "=0.3.3" for gpui-component 0.6.0 compatibility.
- Detail why dual-GPUI resolution fails (conflicting type systems, runtime symbol collisions).
- Record the explicit verdict: NO-GO for external crate dependency; GO for vendored in-tree crate (crates/terminal) adopting the clean modular architecture of gpui-terminal adapted to gpui-pre 0.3.3.
  </action>
  <verify>
    <automated>grep -c "NO-GO" .planning/phases/21-terminal-rendering-alacritty-engine/SPIKE-gpui-terminal.md</automated>
    <fails_when>count is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - SPIKE-gpui-terminal.md exists and documents the version conflict and verdict
  </acceptance_criteria>
  <done>Spike report committed</done>
</task>

<task type="auto">
  <name>Task 2: Workspace dependency setup & crates/terminal scaffold</name>
  <files>desktop-gpui/Cargo.toml, desktop-gpui/crates/terminal/Cargo.toml, desktop-gpui/crates/terminal/src/lib.rs, desktop-gpui/crates/terminal/src/colors.rs</files>
  <read_first>desktop-gpui/Cargo.toml</read_first>
  <action>
1. Update desktop-gpui/Cargo.toml:
   - Add "crates/terminal" to workspace members.
   - Add alacritty_terminal = "0.25.1" to [workspace.dependencies].
   - Add webterm-terminal = { path = "crates/terminal" } to [workspace.dependencies].
   - Add flume = "0.12" to [workspace.dependencies] for lock-free MPSC I/O buffering.

2. Create desktop-gpui/crates/terminal/Cargo.toml:
   - Name: webterm-terminal, version: 0.5.0
   - Dependencies: gpui, gpui-platform, alacritty_terminal, parking_lot, flume, anyhow.

3. Create desktop-gpui/crates/terminal/src/colors.rs:
   - Implement ColorPalette with 16 ANSI colors, foreground, background, cursor, selection colors.
   - Implement conversion from alacritty_terminal::vte::ansi::Color (Named, Spec(Rgb), Indexed) to gpui::Hsla / gpui::Rgba.
   - Provide dark_default() and light_default() palettes matching WebTerm's theme system.

4. Create desktop-gpui/crates/terminal/src/lib.rs exposing the colors module and palette types.
  </action>
  <verify>
    <automated>cargo check -p webterm-terminal --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - crates/terminal compiles cleanly under cargo check
    - ColorPalette resolves 24-bit TrueColor and ANSI indexed colors to GPUI colors
  </acceptance_criteria>
  <done>webterm-terminal crate scaffolded and verified</done>
</task>

</tasks>
