---
title: "Phase 15-02: Frontend UI & Operations (Toolbar, Dialogs, DND integration)"
goal: "Implement SFTP toolbar, operation dialogs, OS drag-and-drop, and progress toasts."
wave: 2
depends_on: ["15-01"]
requirements: [SFTP-01, SFTP-02, SFTP-03, SFTP-04]
must_haves:
  truths:
    - "D-12 Overwrite protection must check file existence before initiating transfer/upload."
    - "D-11/D-14 Dialogs must use shadcn components."
  artifacts:
    - "fe/src/features/sftp/components/DirectoryToolbar.tsx"
    - "fe/src/hooks/use-sftp-transfer.ts"
  key_links:
    - "fe/src/features/sftp/components/DirectoryBrowser.tsx"
---

# 15-02-PLAN: Frontend UI & Operations

## Goal
Implement the user-facing side of SFTP operations with polished UI controls and real-time feedback.

## Tasks

<task>
<action>
Update `sftpApi` and create `useSFTPTransfer` hook.
1. Update `sftpApi.transfer` and `sftpApi.upload` in `fe/src/lib/api.ts` to return `{ transferId: string }`.
2. Implement `useSFTPTransfer` in `fe/src/hooks/use-sftp-transfer.ts`.
   - Function `trackTransfer(transferId: string, fileName: string)`:
     - Opens SSE connection to `/api/sftp/transfer/{id}/progress`.
     - Updates a toast notification with progress bar (D-05).
     - Handles completion/error (D-13, D-15).
</action>
<files>
- fe/src/lib/api.ts
- fe/src/hooks/use-sftp-transfer.ts
</files>
<verify>
Mock SSE stream and verify `useSFTPTransfer` updates toast progress correctly.
</verify>
<done>
`useSFTPTransfer` hook successfully manages SSE subscriptions and displays real-time progress in toasts.
</done>
</task>

<task>
<action>
Implement `DirectoryToolbar.tsx` and integration.
1. Create `DirectoryToolbar.tsx` with buttons for Upload, New Folder, Delete, and Rename (D-01).
2. Create `OperationDialogs.tsx` (or similar) containing `RenameDialog` and `NewFolderDialog` (D-14).
3. Integrate toolbar and dialogs into `DirectoryBrowser.tsx`.
</action>
<files>
- fe/src/features/sftp/components/DirectoryToolbar.tsx
- fe/src/features/sftp/components/OperationDialogs.tsx
- fe/src/features/sftp/components/DirectoryBrowser.tsx
</files>
<verify>
Verify toolbar appears in each pane and buttons open corresponding dialogs/pickers.
</verify>
<done>
Per-pane toolbar implemented with all required actions using shadcn Dialog components.
</done>
</task>

<task>
<action>
Implement OS Drag-and-Drop and Overwrite Protection.
1. Update `DirectoryBrowser.tsx` `onDrop` handler:
   - Handle OS files via `e.dataTransfer.files` (D-04).
   - Check if file exists in current directory before starting upload/transfer (D-12).
   - Show `AlertDialog` for overwrite confirmation if file exists.
2. Call `useSFTPTransfer.trackTransfer` for all uploads and transfers.
</action>
<files>
- fe/src/features/sftp/components/DirectoryBrowser.tsx
</files>
<verify>
Drag file from OS to pane, confirm overwrite if needed, verify progress toast appears.
</verify>
<done>
OS-to-browser drag-and-drop works with overwrite protection and progress tracking integration.
</done>
</task>

<task>
<action>
Final Polish: Context Menu & Destructive Confirmations.
1. Add "Download" to `DirectoryBrowser` context menu (D-03).
2. Replace `window.confirm` for Delete with shadcn `AlertDialog` (D-11).
3. Ensure all errors use destructive toasts (D-13).
</action>
<files>
- fe/src/features/sftp/components/DirectoryBrowser.tsx
</files>
<verify>
Right-click file -> Download works. Delete file -> AlertDialog appears.
</verify>
<done>
Context menu updated with Download, and all destructive operations use AlertDialog.
</done>
</task>
