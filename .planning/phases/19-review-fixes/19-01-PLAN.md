# Phase 19 Plan 1: Address Phase 18 Review Findings

## Overview
Fix issues identified in the code review of Phase 18 (Terminal Engine Selector). This includes fixing prop propagation, race conditions, debug logs, and type safety issues.

## Acceptance Criteria
1. `WTermEngine` correctly respects `cursorBlink` prop.
2. `XTermEngine` correctly respects `theme` and `cursorStyle` props.
3. `WTermEngine` has improved terminal ready handling (no race condition).
4. Debug `console.log` statements are removed or made conditional.
5. Unsafe type assertions in `TerminalPane` are replaced with proper checks.
6. TypeScript compiles without errors.

## Tasks

### 1. Fix Prop Propagation in WTermEngine & XTermEngine
- [x] Update `WTermEngine.tsx` to use `cursorBlink` prop instead of hardcoded `true`.
- [x] Update `XTermEngine.tsx` to correctly apply the `theme` prop to the xterm terminal instance.
- [x] Update `XTermEngine.tsx` to support `cursorStyle` prop.

### 2. Fix Race Conditions & Type Safety
- [x] Refactor `WTermEngine.tsx` ref handling to avoid potential race condition in `handleReady`.
- [x] Fix unsafe type assertions in `TerminalPane.tsx` (e.g., `session!`).
- [x] Replace `any` types in `WTermEngine.tsx` with proper types or interfaces.

### 3. Cleanup & Polish
- [x] Remove or conditionalize debug `console.log` statements in `TerminalPane.tsx` and `useSSHSession.ts`.
- [x] Ensure all useEffect dependencies are complete (e.g., `session` in `TerminalPane.tsx`).

## Verification Plan

### Automated Tests
- Run `npm run build` in `fe` to ensure no TypeScript or build errors.
- (Optional) Run `go build ./...` in `be` to ensure backend remains stable.

### Manual Verification (Mental Check)
- Verify `cursorBlink` prop flow: `SettingsPage` -> `useSettings` -> `TerminalPane` -> `TerminalWrapper` -> `WTermEngine`/`XTermEngine`.
- Verify `theme` prop flow: `SettingsPage` -> `useSettings` -> `TerminalPane` -> `TerminalWrapper` -> `XTermEngine`.
- Verify `cursorStyle` prop flow: `SettingsPage` -> `useSettings` -> `TerminalPane` -> `TerminalWrapper` -> `XTermEngine`.
