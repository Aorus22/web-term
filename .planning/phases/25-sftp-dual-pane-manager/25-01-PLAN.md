---
phase: 25-sftp-dual-pane-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop-gpui/crates/backend-client/src/types.rs
  - desktop-gpui/crates/backend-client/src/rest.rs
  - desktop-gpui/crates/backend-client/tests/sftp_api_test.rs
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/src/views/nav.rs
  - desktop-gpui/crates/webterm/src/views/sftp.rs
  - desktop-gpui/crates/webterm/tests/sftp_view_test.rs
files_deleted: []
autonomous: true
requirements: [SFTP-01, SFTP-02]
user_setup: []
must_haves:
  truths:
    - "BackendClient provides typed SFTP methods for directory listing, mkdir, rename, remove, download, upload, and transfer querying"
    - "Contract tests validate SFTP REST API wire encoding and response decoding against mock server"
    - "Sidebar navigation includes Files view button switching main area to dual-pane SFTP manager"
    - "Dual-pane view renders Left and Right panes with independent source selection (Local Filesystem vs saved SSH hosts)"
    - "Directory browser displays clickable breadcrumbs, file metadata columns, and sorting with folders on top"
  artifacts:
    - path: "desktop-gpui/crates/backend-client/src/types.rs"
      provides: "SftpFileInfo, SftpTransferStatus DTOs"
      min_lines: 100
    - path: "desktop-gpui/crates/webterm/src/views/sftp.rs"
      provides: "Dual-pane SFTP file browser view"
      min_lines: 200
  key_links:
    - from: "desktop-gpui/crates/webterm/src/views/sftp.rs"
      to: "desktop-gpui/crates/backend-client/src/rest.rs"
      via: "sftp_list queries"
      pattern: "sftp_list"
---

<objective>
Implement typed SFTP REST client endpoints and models in `backend-client`, and create the dual-pane SFTP manager in `webterm` with independent source selection, directory browsing, interactive breadcrumbs, and metadata sorting (`SFTP-01`, `SFTP-02`).

Purpose: Deliver the foundational dual-pane layout and responsive directory browser in GPUI.
Output: Extended `types.rs`, `rest.rs`, new `views/sftp.rs`, `views/nav.rs` integration, and test suites.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-sftp-dual-pane-manager/25-CONTEXT.md
@.planning/phases/25-sftp-dual-pane-manager/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Implement SFTP DTOs and BackendClient REST methods</name>
<files>desktop-gpui/crates/backend-client/src/types.rs, desktop-gpui/crates/backend-client/src/rest.rs, desktop-gpui/crates/backend-client/tests/sftp_api_test.rs</files>
<read_first>desktop-gpui/crates/backend-client/src/types.rs, desktop-gpui/crates/backend-client/src/rest.rs, be/internal/api/sftp.go</read_first>
<action>
1. In `desktop-gpui/crates/backend-client/src/types.rs`:
   - Add `SftpFileInfo`:
     - `name: String`
     - `size: i64`
     - `mode: String`
     - `mod_time: String`
     - `is_dir: bool`
   - Add `SftpTransferStatus`:
     - `id: String`
     - `bytes_transferred: i64`
     - `total_bytes: i64`
     - `status: String`
     - `error: Option<String>`
2. In `desktop-gpui/crates/backend-client/src/rest.rs`:
   - Add `sftp_list(&self, connection_id: &str, path: &str) -> Result<Vec<SftpFileInfo>, BackendClientError>`
   - Add `sftp_mkdir(&self, connection_id: &str, path: &str) -> Result<(), BackendClientError>`
   - Add `sftp_rename(&self, connection_id: &str, old_path: &str, new_path: &str) -> Result<(), BackendClientError>`
   - Add `sftp_remove(&self, connection_id: &str, path: &str) -> Result<(), BackendClientError>`
   - Add `sftp_download(&self, connection_id: &str, path: &str) -> Result<Vec<u8>, BackendClientError>`
   - Add `sftp_upload(&self, connection_id: &str, path: &str, filename: &str, data: Vec<u8>) -> Result<String, BackendClientError>`
   - Add `sftp_list_transfers(&self) -> Result<Vec<SftpTransferStatus>, BackendClientError>`
3. In `desktop-gpui/crates/backend-client/tests/sftp_api_test.rs`:
   - Add contract tests for `sftp_list`, `sftp_mkdir`, `sftp_rename`, `sftp_remove`, and `sftp_list_transfers` against a mock HTTP server.
</action>
<verify>
cargo test --test sftp_api_test --manifest-path desktop-gpui/crates/backend-client/Cargo.toml
</verify>
<done>
Backend client SFTP methods and contract tests compile and pass.
</done>
</task>

<task type="auto">
<name>Task 2: Implement Dual-Pane SFTP View and Directory Browsing with Breadcrumbs and Sorting</name>
<files>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/views/nav.rs, desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/webterm/tests/sftp_view_test.rs</files>
<read_first>desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/webterm/src/views/nav.rs, desktop-gpui/crates/webterm/src/views/hosts.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Add `show_sftp_manager: bool` flag to route between terminal tabs, hosts catalog, and files view.
   - Add `sftp_manager: SftpManager` holding `left_pane: SftpPaneState` and `right_pane: SftpPaneState`.
   - Implement `toggle_sftp_manager(&mut self, cx: &mut Context<Self>)`.
   - Implement pane navigation methods: `sftp_navigate(pane: SftpActivePane, path: String, cx: &mut Context<Self>)`, `sftp_set_source(pane: SftpActivePane, conn_id: String, cx: &mut Context<Self>)`.
2. In `desktop-gpui/crates/webterm/src/views/nav.rs`:
   - Add `"📁 Files"` button in the sidebar rail toggling `show_sftp_manager`.
3. In `desktop-gpui/crates/webterm/src/views/sftp.rs`:
   - Render dual-pane layout: Left Pane (50%) and Right Pane (50%) separated by a vertical border.
   - For each pane:
     - Header bar with Source Selector dropdown (Local Machine vs saved connections from `app.connections`).
     - Breadcrumb bar with clickable directory segments, "⬆ Up" button, and refresh button.
     - Search / filter input and "Show Hidden" checkbox.
     - Table header with clickable sort indicators: Name, Size, Modified Date.
     - Scrollable list of files and folders with icons (📁 for folder, 📄 for file), formatted sizes, and selection highlights.
     - Double-clicking a folder navigates into it.
4. In `desktop-gpui/crates/webterm/tests/sftp_view_test.rs`:
   - Unit tests verifying breadcrumb segment splitting, sorting logic (directories first, ascending/descending), and hidden file filtering.
</action>
<verify>
cargo test --workspace --manifest-path desktop-gpui/Cargo.toml
cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings
</verify>
<done>
Dual-pane layout renders properly, breadcrumbs and sorting are fully tested, and all workspace checks pass.
</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace --manifest-path desktop-gpui/Cargo.toml`
Run `cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings`
</verification>
