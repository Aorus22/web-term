---
phase: 25-sftp-dual-pane-manager
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - desktop-gpui/crates/webterm/src/views/sftp.rs
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/tests/sftp_operations_test.rs
files_deleted: []
autonomous: true
requirements: [SFTP-03]
user_setup: []
must_haves:
  truths:
    - "User can create new folders via interactive modal prompt"
    - "User can rename files and directories via interactive modal prompt"
    - "User can delete files and directories with confirmation prompt"
    - "User can upload and download files with streaming transfer progress"
    - "Collapsible transfer drawer displays active transfer progress, byte counts, and status"
  artifacts:
    - path: "desktop-gpui/crates/webterm/src/views/sftp.rs"
      provides: "New Folder, Rename, Delete modals and bottom Transfer Progress Drawer"
      min_lines: 300
    - path: "desktop-gpui/crates/webterm/tests/sftp_operations_test.rs"
      provides: "Unit tests verifying file operation validation and transfer progress tracking"
      min_lines: 80
  key_links:
    - from: "desktop-gpui/crates/webterm/src/views/sftp.rs"
      to: "desktop-gpui/crates/backend-client/src/rest.rs"
      via: "sftp_mkdir, sftp_rename, sftp_remove, sftp_upload, sftp_download"
      pattern: "sftp_mkdir|sftp_rename|sftp_remove"
---

<objective>
Implement core file management operations (New Folder, Rename, Delete) with modal dialogs and streaming upload/download transfers accompanied by a bottom transfer progress drawer (`SFTP-03`).

Purpose: Provide full file manipulation and streaming progress tracking in the dual-pane file manager.
Output: Extended `views/sftp.rs`, `app_state.rs`, and new test suite `tests/sftp_operations_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-sftp-dual-pane-manager/25-CONTEXT.md
@.planning/phases/25-sftp-dual-pane-manager/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Add New Folder, Rename, and Delete modal dialogs and operations</name>
<files>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/app_state.rs</files>
<read_first>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/app_state.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Add modal state enum `SftpModalState`:
     - `NewFolder { pane: SftpActivePane, dir_path: String, folder_name: String }`
     - `Rename { pane: SftpActivePane, old_path: String, new_name: String }`
     - `DeleteConfirm { pane: SftpActivePane, path: String, is_dir: bool }`
   - Implement operation handlers:
     - `sftp_create_folder(&mut self, pane: SftpActivePane, name: &str, cx: &mut Context<Self>)`
     - `sftp_rename_entry(&mut self, pane: SftpActivePane, old_path: &str, new_name: &str, cx: &mut Context<Self>)`
     - `sftp_delete_entry(&mut self, pane: SftpActivePane, path: &str, cx: &mut Context<Self>)`
   - Ensure upon successful completion, the affected pane automatically refreshes directory contents.
2. In `desktop-gpui/crates/webterm/src/views/sftp.rs`:
   - Add toolbar buttons for "📁+ New Folder", "✏️ Rename", "🗑️ Delete", "🔄 Refresh".
   - Render modal overlays when `sftp_manager.modal_state` is set, with text inputs, Confirm, and Cancel buttons.
</action>
<verify>
cargo check --workspace --manifest-path desktop-gpui/Cargo.toml
</verify>
<done>
File operation modals and handlers compile and function cleanly.
</done>
</task>

<task type="auto">
<name>Task 2: Implement streaming transfers and bottom transfer drawer</name>
<files>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/webterm/tests/sftp_operations_test.rs</files>
<read_first>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/app_state.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Implement `sftp_upload_file(&mut self, target_pane: SftpActivePane, filename: String, data: Vec<u8>, cx: &mut Context<Self>)`
   - Implement `sftp_download_file(&mut self, source_pane: SftpActivePane, file_path: String, cx: &mut Context<Self>)`
   - Implement `sftp_poll_transfers(&mut self, cx: &mut Context<Self>)`
   - Track active transfers in `sftp_manager.active_transfers`.
2. In `desktop-gpui/crates/webterm/src/views/sftp.rs`:
   - Render collapsible bottom Transfer Drawer with header (`Transfers (N)`, Expand/Collapse toggle).
   - When expanded, list each transfer:
     - Filename and transfer direction indicator.
     - Progress bar (`bytes_transferred / total_bytes * 100%`).
     - Status tag (Pending, Transferring, Completed, Error).
3. In `desktop-gpui/crates/webterm/tests/sftp_operations_test.rs`:
   - Unit tests validating transfer progress calculation, modal input validation (e.g. empty folder name disallowed), and deletion confirmation flow.
</action>
<verify>
cargo test --workspace --manifest-path desktop-gpui/Cargo.toml
cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings
</verify>
<done>
Transfer progress drawer and tests compile and pass with zero warnings.
</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace --manifest-path desktop-gpui/Cargo.toml`
Run `cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings`
</verification>
