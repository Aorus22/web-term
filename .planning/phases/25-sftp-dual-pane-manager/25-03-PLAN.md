---
phase: 25-sftp-dual-pane-manager
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - desktop-gpui/crates/webterm/src/views/sftp.rs
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/tests/sftp_integration_test.rs
files_deleted: []
autonomous: true
requirements: [SFTP-04]
user_setup: []
must_haves:
  truths:
    - "User can initiate inter-pane transfers between Left and Right panes"
    - "Drag-and-drop between panes initiates streaming transfer into target destination folder"
    - "Right-click context menu provides quick actions for Transfer, Rename, and Delete"
    - "Keyboard shortcuts (Enter, F2, Delete, F5) operate on active pane selections"
    - "End-to-end integration test exercises real backend supervisor SFTP filesystem operations"
  artifacts:
    - path: "desktop-gpui/crates/webterm/tests/sftp_integration_test.rs"
      provides: "End-to-end SFTP manager integration test suite against supervisor"
      min_lines: 80
  key_links:
    - from: "desktop-gpui/crates/webterm/src/views/sftp.rs"
      to: "desktop-gpui/crates/webterm/src/app_state.rs"
      via: "sftp_transfer_between_panes"
      pattern: "sftp_transfer_between_panes"
---

<objective>
Implement inter-pane transfers, drag-and-drop interactions, right-click context menus, and keyboard shortcuts in the dual-pane SFTP manager, validated by an end-to-end integration test suite (`SFTP-04`).

Purpose: Complete full interaction parity for desktop SFTP management.
Output: Extended `views/sftp.rs`, `app_state.rs`, and new integration test `tests/sftp_integration_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-sftp-dual-pane-manager/25-CONTEXT.md
@.planning/phases/25-sftp-dual-pane-manager/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Add inter-pane transfer actions, context menus, and keyboard shortcuts</name>
<files>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/app_state.rs</files>
<read_first>desktop-gpui/crates/webterm/src/views/sftp.rs, desktop-gpui/crates/webterm/src/app_state.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Implement `sftp_transfer_selected(&mut self, from_pane: SftpActivePane, cx: &mut Context<Self>)`:
     - Reads selected items from `from_pane`.
     - Identifies target destination path in the opposite pane.
     - Initiates streaming transfer from source connection to destination connection.
     - Notifies transfer queue.
2. In `desktop-gpui/crates/webterm/src/views/sftp.rs`:
   - Add central "Transfer ➔" and "⬅ Transfer" buttons in the divider toolbar between the two panes.
   - Support right-click context menu on file rows:
     - "Transfer to opposite pane"
     - "Rename"
     - "Delete"
     - "Copy Path"
   - Add keyboard navigation support:
     - `Enter`: Enter directory
     - `Backspace` / `Alt+Up`: Navigate to parent directory
     - `F2`: Trigger rename modal
     - `Delete`: Trigger delete confirmation
     - `F5`: Refresh active pane
</action>
<verify>
cargo check --workspace --manifest-path desktop-gpui/Cargo.toml
</verify>
<done>
Inter-pane transfer actions, context menus, and keyboard shortcuts compile cleanly.
</done>
</task>

<task type="auto">
<name>Task 2: Implement end-to-end integration test against real supervisor backend</name>
<files>desktop-gpui/crates/webterm/tests/sftp_integration_test.rs</files>
<read_first>desktop-gpui/crates/webterm/tests/local_smoke_test.rs, desktop-gpui/crates/backend-client/src/rest.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/tests/sftp_integration_test.rs`:
   - Locate test backend fixture (gracefully skip if absent).
   - Start real backend supervisor on loopback port.
   - Use `BackendClient` to perform SFTP operations on local filesystem (`connection_id = "local"`):
     - List temp directory via `sftp_list`.
     - Create a subdirectory via `sftp_mkdir`.
     - Verify subdirectory appears in `sftp_list`.
     - Rename subdirectory via `sftp_rename`.
     - Remove directory via `sftp_remove`.
     - Verify clean removal.
   - Cleanly stop backend supervisor.
2. Run full workspace test suite.
</action>
<verify>
cargo test --test sftp_integration_test --manifest-path desktop-gpui/crates/webterm/Cargo.toml
cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings
</verify>
<done>
End-to-end integration test passes and clippy produces zero warnings.
</done>
</task>

</tasks>

<verification>
Run `cargo test --workspace --manifest-path desktop-gpui/Cargo.toml`
Run `cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings`
</verification>
