# Plan 27-01: Parity Audit Matrix Execution vs Validated List

## Frontmatter
- **Phase:** 27-parity-audit-desktop-hardening
- **Plan:** 27-01
- **Requirement:** QA-02 (A parity audit verifies every validated v0.2–v0.4 requirement against the desktop app in a recorded audit matrix)
- **Status:** Ready

## Objective
Execute a comprehensive parity audit comparing the desktop GPUI client against all 22 validated requirements from `PROJECT.md` (v0.2.0, v0.3.0, and v0.4.0) and all 30 v0.5.0 requirements:
1. Create `.planning/phases/27-parity-audit-desktop-hardening/PARITY-MATRIX.md` recording:
   - Feature name & description.
   - Origin milestone/phase in web application.
   - Corresponding desktop crate, module, and view implementation.
   - Concrete automated test suite and manual verification criteria.
   - Status (Parity Achieved / Verified).
2. Create automated parity contract tests in `desktop-gpui/crates/webterm/tests/parity_audit_test.rs` validating that:
   - All REST API endpoints and data models in the desktop client map to the backend API contracts.
   - All sidebar views (`Hosts`, `Keys`, `SFTP`, `Forwards`, `Settings`) are routable and have associated handlers.
   - Terminal sessions support both Local Shell and SSH targets with multi-tab management, status pills, and theme propagation.
   - SSH key authentication handles passphrase prompting, session caching, and auth method toggling.
   - Dual-pane SFTP file management supports local and remote sources with file CRUD, streaming transfers, and drag-and-drop.
   - Port forwarding supports local and reverse SSH tunnels with lifecycle actions.
   - Settings enforces fixed Alacritty engine (no engine selector dropdown) with dark/light themes and custom backend override.

## Proposed Changes

### Component: `.planning`
#### [NEW] `.planning/phases/27-parity-audit-desktop-hardening/PARITY-MATRIX.md`
- Detail all 22 validated web requirements and 30 desktop requirements with full verification evidence.

### Component: `webterm`
#### [NEW] `desktop-gpui/crates/webterm/tests/parity_audit_test.rs`
- Automated verification checking state invariants, route completeness, and data structure parity.

## Verification Plan
1. `cargo test --package webterm --test parity_audit_test`
2. `cargo clippy --package webterm --test parity_audit_test -- -D warnings`
3. Inspect `PARITY-MATRIX.md` for 100% requirement coverage.
