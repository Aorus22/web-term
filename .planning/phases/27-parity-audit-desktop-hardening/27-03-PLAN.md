# Plan 27-03: Packaging, Launchable Bundle & Loopback Enforcement

## Frontmatter
- **Phase:** 27-parity-audit-desktop-hardening
- **Plan:** 27-03
- **Requirement:** QA-03 (App ships as a single launchable bundle with the backend bound to loopback only)
- **Status:** Ready

## Objective
Enforce strict loopback binding on the backend supervisor and provide single launchable bundle packaging for Windows and Linux:
1. **Loopback Enforcement (Backend & Supervisor):**
   - Add `Host` field to Go `config.Config` loaded from `WEBTERM_HOST` (defaulting to `"0.0.0.0"` for web backwards compatibility).
   - In `be/cmd/server/main.go`, construct bind address with `net.JoinHostPort(cfg.Host, port)` so setting `WEBTERM_HOST=127.0.0.1` strictly binds the server to loopback only.
   - In `desktop-gpui/crates/supervisor/src/lib.rs`, add `("WEBTERM_HOST", "127.0.0.1".to_string())` to `SpawnOptions::env_vars()`.
   - Add integration test `desktop-gpui/crates/supervisor/tests/loopback_test.rs` validating that backend binds strictly to `127.0.0.1`.
2. **Bundled Binary Discovery:**
   - In `desktop-gpui/crates/webterm/src/main.rs`, update `resolve_backend_path` to look adjacent to `std::env::current_exe()`, ensuring the desktop client finds `backend.exe` / `backend` in the installed/portable bundle directory before falling back to development search paths.
   - Add test `desktop-gpui/crates/webterm/tests/bundle_resolution_test.rs`.
3. **Packaging Tooling (Windows & Linux):**
   - Create `desktop-gpui/scripts/package-windows.cmd` and `desktop-gpui/scripts/package-windows.ps1` for building and assembling a portable Windows x64 distribution bundle (`dist/webterm-windows-x64/`).
   - Create `desktop-gpui/scripts/package-linux.sh` for assembling Linux x64 distribution bundle (`dist/webterm-linux-x64/`).

## Proposed Changes

### Component: `backend`
#### [MODIFY] `be/internal/config/config.go`
- Add `Host: getEnv("WEBTERM_HOST", "0.0.0.0")` to `Config`.

#### [MODIFY] `be/cmd/server/main.go`
- Bind listener using `net.JoinHostPort(cfg.Host, port)`.

### Component: `supervisor`
#### [MODIFY] `desktop-gpui/crates/supervisor/src/lib.rs`
- Inject `WEBTERM_HOST=127.0.0.1` into child process environment.

#### [NEW] `desktop-gpui/crates/supervisor/tests/loopback_test.rs`
- Verify backend listener binds strictly to loopback IP (`127.0.0.1`).

### Component: `webterm`
#### [MODIFY] `desktop-gpui/crates/webterm/src/main.rs`
- Add `current_exe().parent()` resolution to `resolve_backend_path`.

#### [NEW] `desktop-gpui/crates/webterm/tests/bundle_resolution_test.rs`
- Test bundled executable resolution adjacent to binary.

### Component: `scripts`
#### [NEW] `desktop-gpui/scripts/package-windows.cmd`
#### [NEW] `desktop-gpui/scripts/package-windows.ps1`
#### [NEW] `desktop-gpui/scripts/package-linux.sh`

## Verification Plan
1. `go test ./...` in `be/`
2. `cargo test --manifest-path desktop-gpui/Cargo.toml --package webterm-supervisor --test loopback_test`
3. `cargo test --manifest-path desktop-gpui/Cargo.toml --package webterm --test bundle_resolution_test`
4. Build test backend with updated Go code and verify loopback listener.
