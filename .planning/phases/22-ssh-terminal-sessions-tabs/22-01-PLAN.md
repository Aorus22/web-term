---
phase: 22-ssh-terminal-sessions-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop-gpui/Cargo.toml
  - desktop-gpui/crates/backend-client/Cargo.toml
  - desktop-gpui/crates/backend-client/src/terminal_ws.rs
  - desktop-gpui/crates/backend-client/src/lib.rs
  - desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs
files_deleted: []
autonomous: true
requirements: [SHELL-04, TERM-06]
user_setup: []
must_haves:
  truths:
    - "TerminalWsClient connects to backend /ws and conducts handshake (connect or attach)"
    - "Binary frames are streamed bidirectionally without corruption or encoding issues"
    - "Control JSON frames (resize, ready, disconnect) are formatted and dispatched accurately"
  artifacts:
    - path: "desktop-gpui/crates/backend-client/src/terminal_ws.rs"
      provides: "WebSocket terminal transport client with framing and async message pumps"
      min_lines: 150
    - path: "desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs"
      provides: "Integration contract tests for WebSocket terminal protocol"
      min_lines: 80
  key_links:
    - from: "desktop-gpui/crates/backend-client/src/lib.rs"
      to: "desktop-gpui/crates/backend-client/src/terminal_ws.rs"
      via: "exports TerminalWsClient and message types"
      pattern: "TerminalWsClient"
---

<objective>
Implement the WebSocket terminal transport client in `webterm-backend-client` to bridge GPUI terminal views to the backend PTY and SSH sessions.

Purpose: Provide a reliable, typed bidirectional transport speaking the backend's `/ws` binary byte-stream and JSON control protocol.
Output: `terminal_ws.rs`, updated `Cargo.toml`, and passing integration tests in `terminal_ws_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-CONTEXT.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tokio-tungstenite and implement TerminalWsClient</name>
  <files>desktop-gpui/Cargo.toml, desktop-gpui/crates/backend-client/Cargo.toml, desktop-gpui/crates/backend-client/src/terminal_ws.rs, desktop-gpui/crates/backend-client/src/lib.rs</files>
  <read_first>be/internal/ssh/proxy.go, desktop-gpui/crates/backend-client/src/lib.rs</read_first>
  <action>
1. Update `desktop-gpui/Cargo.toml`:
   - Add exact dependency pins:
     `tokio-tungstenite = { version = "=0.26.2", default-features = false, features = ["connect", "handshake"] }`
     `futures-util = "=0.3.31"`
2. Update `desktop-gpui/crates/backend-client/Cargo.toml`:
   - Add `tokio-tungstenite = { workspace = true }`, `futures-util = { workspace = true }`, `flume = { workspace = true }`.
3. Implement `desktop-gpui/crates/backend-client/src/terminal_ws.rs`:
   - Protocol types: `WsConnectRequest`, `WsAttachRequest`, `WsServerResponse`, `WsResizeRequest`.
   - `TerminalWsHandle`: provides `send_input(bytes)`, `resize(cols, rows)`, `disconnect()`, `recv_output()`, `status()`.
   - `TerminalWsClient::connect(url, request)`: performs handshake, handles `"connected"` response, sends `"ready"`, and spawns read/write pump tasks over `flume` channels.
   - `TerminalWsClient::attach(url, session_id)`: performs attach handshake and spawns pumps.
4. Export `TerminalWsClient`, `TerminalWsHandle`, and protocol structs in `desktop-gpui/crates/backend-client/src/lib.rs`.
  </action>
  <verify>
    <automated>cargo check -p webterm-backend-client --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo check -p webterm-backend-client` succeeds with 0 errors
  </acceptance_criteria>
  <done>WebSocket terminal transport client implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement integration contract test</name>
  <files>desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs</files>
  <read_first>desktop-gpui/crates/backend-client/src/terminal_ws.rs</read_first>
  <action>
1. Create `desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs`:
   - Spin up a local mock WebSocket server using `tokio_tungstenite` on an ephemeral port.
   - Verify:
     a. `TerminalWsClient::connect` sends JSON connect handshake and receives `"connected"`.
     b. Client sends `"ready"` upon connected.
     c. Server binary messages are received through the output channel.
     d. Client input sent via `send_input` is transmitted as binary frames to the server.
     e. Resize commands are transmitted as JSON text frames to the server.
     f. `TerminalWsClient::attach` sends JSON attach handshake and reconnects.
  </action>
  <verify>
    <automated>cargo test -p webterm-backend-client --test terminal_ws_test --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cargo clippy -p webterm-backend-client --manifest-path desktop-gpui/Cargo.toml -- -D warnings</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `terminal_ws_test` passes all tests
    - clippy reports 0 warnings
  </acceptance_criteria>
  <done>WebSocket terminal transport contract verified</done>
</task>

</tasks>
