---
phase: 22-ssh-terminal-sessions-tabs
plan: 04
type: execute
wave: 4
depends_on: [22-01, 22-02, 22-03]
files_modified:
  - desktop-gpui/crates/webterm/src/views/new_tab_modal.rs
  - desktop-gpui/crates/webterm/src/views/mod.rs
  - desktop-gpui/crates/webterm/src/views/nav.rs
  - desktop-gpui/crates/webterm/src/views/tab_strip.rs
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/tests/local_tab_test.rs
files_deleted: []
autonomous: true
requirements: [TERM-05]
user_setup: []
must_haves:
  truths:
    - "User can open a first-class Local Terminal tab launching local shell via backend /ws"
    - "New tab launcher modal allows choosing between Local Shell and Quick SSH Connect"
    - "Local tab sends initial sizing and receives raw PTY stream without errors"
  artifacts:
    - path: "desktop-gpui/crates/webterm/src/views/new_tab_modal.rs"
      provides: "New tab launcher modal for choosing Local Shell or Quick SSH session"
      min_lines: 80
    - path: "desktop-gpui/crates/webterm/tests/local_tab_test.rs"
      provides: "Contract tests for local terminal session protocol and new tab launcher"
      min_lines: 60
  key_links:
    - from: "desktop-gpui/crates/webterm/src/views/nav.rs"
      to: "desktop-gpui/crates/webterm/src/views/new_tab_modal.rs"
      via: "renders new tab modal when show_new_tab_modal is true"
      pattern: "render_new_tab_modal"
---

<objective>
Implement first-class Local Terminal tab launching (`TERM-05`) with a New Tab launcher modal (Local Shell vs. Quick SSH Connect).

Purpose: Complete `TERM-05` and provide an intuitive workflow for spawning local shells and ad-hoc SSH tabs.
Output: `new_tab_modal.rs`, updated tab strip / nav shell, integration tests in `local_tab_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-CONTEXT.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-RESEARCH.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-01-SUMMARY.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-02-SUMMARY.md
@.planning/phases/22-ssh-terminal-sessions-tabs/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement New Tab launcher modal (Local Shell vs Quick SSH)</name>
  <files>desktop-gpui/crates/webterm/src/views/new_tab_modal.rs, desktop-gpui/crates/webterm/src/views/mod.rs, desktop-gpui/crates/webterm/src/views/tab_strip.rs, desktop-gpui/crates/webterm/src/views/nav.rs, desktop-gpui/crates/webterm/src/app_state.rs</files>
  <read_first>desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/webterm/src/views/tab_strip.rs</read_first>
  <action>
1. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Add `pub show_new_tab_modal: bool` to `AppState`.
   - Add fields for Quick SSH inputs (or modal state): `new_tab_host: String`, `new_tab_user: String`, `new_tab_port: String`, `new_tab_password: String`.
   - Add `pub fn toggle_new_tab_modal(&mut self, cx: &mut Context<Self>)`.
   - Update `Ctrl+T` and '+' click: if modal is closed, opens modal; or directly spawns local tab when requested.
2. Create `desktop-gpui/crates/webterm/src/views/new_tab_modal.rs`:
   - Modal overlay centered over content area:
     - Card: "New Terminal Tab".
     - Choice 1: "💻 Local Shell (Default)" - 1 click or Enter to launch `open_local_tab`.
     - Choice 2: "🌐 Quick SSH Connection" - input fields for Host, User, Port (default 22), Password, with "Connect" button.
     - "Cancel" or Escape to close modal.
3. In `desktop-gpui/crates/webterm/src/views/nav.rs`:
   - When `app.show_new_tab_modal` is true, render `render_new_tab_modal(app, cx)` overlay over the main content area.
  </action>
  <verify>
    <automated>cargo check -p webterm --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `new_tab_modal.rs` compiles cleanly and renders modal overlay
  </acceptance_criteria>
  <done>New Tab modal and Local Shell / Quick SSH launcher implemented</done>
</task>

<task type="auto">
  <name>Task 2: Local terminal contract and integration tests</name>
  <files>desktop-gpui/crates/webterm/tests/local_tab_test.rs</files>
  <read_first>desktop-gpui/crates/webterm/src/session.rs, desktop-gpui/crates/backend-client/src/terminal_ws.rs</read_first>
  <action>
1. Create `desktop-gpui/crates/webterm/tests/local_tab_test.rs`:
   - Test `WsConnectRequest::for_local(80, 24)` structure and JSON serialization:
     - `type == "connect"`, `session_type == "local"`, `connection_id == "local"`.
   - Test mock WebSocket server interaction for local shell session:
     - Connect handshake with local session.
     - Server assigns `session-local-xyz`.
     - Ready control message received.
     - Streaming binary data from local shell PTY.
     - Terminal resize forwarding.
     - Terminal disconnect.
  </action>
  <verify>
    <automated>cargo test -p webterm --test local_tab_test --manifest-path desktop-gpui/Cargo.toml</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `local_tab_test` passes all tests
    - 0 warnings across workspace from clippy
  </acceptance_criteria>
  <done>Local terminal session contract verified</done>
</task>

</tasks>
