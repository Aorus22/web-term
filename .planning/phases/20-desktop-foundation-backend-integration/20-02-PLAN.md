---
phase: 20-desktop-foundation-backend-integration
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - desktop-gpui/crates/settings/src/lib.rs
  - desktop-gpui/crates/settings/src/paths.rs
  - desktop-gpui/crates/settings/tests/store_test.rs
  - desktop-gpui/crates/backend-client/src/lib.rs
  - desktop-gpui/crates/backend-client/src/rest.rs
  - desktop-gpui/crates/backend-client/src/types.rs
files_deleted: []
autonomous: true
requirements: [SHELL-01]
user_setup: []
must_haves:
  truths:
    - "Settings store persists theme, window_state, and backend_path across store drop + reopen; corrupt JSON regenerates defaults and preserves the old file as settings.json.bak"
    - "Settings file carries user-only permissions; the stored encryption_key is 64 lowercase hex chars and is stable across reloads"
    - "backend-client get_settings/list_connections parse live JSON; is_ready returns false on connection-refused"
  artifacts:
    - path: "desktop-gpui/crates/settings/src/lib.rs"
      provides: "DesktopSettings load/save with corrupt-file recovery and stable encryption-key custody"
      min_lines: 80
      exports: ["DesktopSettings", "Theme", "WindowState"]
    - path: "desktop-gpui/crates/settings/src/paths.rs"
      provides: "config dir resolution (webterm-desktop) with test-injectable base"
      min_lines: 30
    - path: "desktop-gpui/crates/backend-client/src/rest.rs"
      provides: "typed REST client: get_settings, list_connections, is_ready"
      min_lines: 60
      exports: ["BackendClient"]
  key_links:
    - from: "desktop-gpui/crates/settings/src/lib.rs"
      to: "desktop-gpui/crates/supervisor/src/lib.rs"
      via: "DesktopSettings supplies encryption_key and db_path consumed as SpawnOptions (key travels via env, never argv) — wiring completed in plan 20-03"
      pattern: "encryption_key|db_path"
---

<objective>
Implement the two remaining headless crates: the desktop settings store (persistence, corrupt-recovery, stable encryption-key custody, per-OS permissions) and the typed backend-client skeleton (readiness + representative calls).

Purpose: The settings store fixes the backend's random-per-session encryption key problem; the client establishes the DTO-mirroring seam later phases expand.
Output: webterm-settings and webterm-backend-client with passing tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-desktop-foundation-backend-integration/20-RESEARCH.md
@.planning/phases/20-desktop-foundation-backend-integration/20-01-SUMMARY.md
@be/internal/api/settings.go
@be/internal/api/connections.go
@be/internal/db/models.go
</context>

<prohibitions>
- Prohibition 1: The desktop settings store must never hold SSH connection or key material (backend's encrypted SQLite owns those). The backend decryption key is the deliberate single exception.
- Prohibition 2: The encryption key is never logged, never passed via argv; stored with user-only permissions.
</prohibitions>

<tasks>

<task type="auto">
  <name>Task 1: Settings store — persistence, recovery, key custody</name>
  <files>desktop-gpui/crates/settings/src/lib.rs, desktop-gpui/crates/settings/src/paths.rs, desktop-gpui/crates/settings/tests/store_test.rs</files>
  <read_first>be/internal/config/config.go, .planning/phases/20-desktop-foundation-backend-integration/20-RESEARCH.md</read_first>
  <action>
Implement crates/settings (webterm-settings), replacing the 20-01 stub:

- `pub struct DesktopSettings { backend_path: Option<PathBuf>, encryption_key: Option<String>, theme: Theme, window_state: Option<WindowState>, last_backend_url: Option<String> }`; `pub enum Theme { Dark, Light, System }` (serde lowercase); `pub struct WindowState { x: i32, y: i32, width: u32, height: u32, maximized: bool }` (fields Option-friendly for first run).
- paths.rs: `config_dir() -> PathBuf` = `dirs::config_dir()/webterm-desktop` (Windows %APPDATA%, Linux ~/.config); `settings_path()`, `db_path()` = config_dir/webterm.db; `ensure_dirs()`; all path functions take an injectable base for tests.
- Load/save serde_json pretty. Corrupt/missing → rename corrupt file to settings.json.bak, regenerate defaults (never silently delete user data).
- `ensure_encryption_key(&mut self) -> String` — load-or-generate: None → generate 32 random bytes (getrandom/rand), hex-encode 64 chars, persist immediately, return; else return stored key unchanged (STABLE across restarts — fixes the backend's random-per-session key behavior in be/internal/config/config.go).
- Permissions: Unix 0o600 via PermissionsExt (`#[cfg(unix)]`); Windows no-op (profile ACLs) — documented `#[cfg(windows)]`.

Tests (tests/store_test.rs) against tempdir-injected base: roundtrip persist; corrupt→bak+defaults; key stability (two consecutive ensure_encryption_key calls return equal values); key format `^[0-9a-f]{64}$`.
  </action>
  <verify>
    <automated>cd desktop-gpui && cargo test -p webterm-settings</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - settings/src/lib.rs contains "ensure_encryption_key" and the 64-hex format guard
    - paths.rs contains "webterm-desktop"
    - store_test.rs asserts key stability across consecutive calls
  </acceptance_criteria>
  <done>Settings store proven: persistence, recovery, stable key custody, per-OS permissions</done>
</task>

<task type="auto">
  <name>Task 2: Backend-client skeleton — typed REST with readiness probe</name>
  <files>desktop-gpui/crates/backend-client/src/lib.rs, desktop-gpui/crates/backend-client/src/rest.rs, desktop-gpui/crates/backend-client/src/types.rs</files>
  <read_first>be/internal/api/settings.go, be/internal/api/connections.go, be/internal/db/models.go</read_first>
  <action>
Implement crates/backend-client (webterm-backend-client), replacing the 20-01 stub:

- types.rs: mirror backend DTOs read from be/internal/db/models.go + handlers — `Connection` (field-for-field with the Go model's JSON tags for the fields Phase 20 call sites need; TODO marker to expand in Phase 22/23), `Settings` (whatever GET /api/settings returns — read the handler first).
- rest.rs: `pub struct BackendClient { base_url: String, http: reqwest::Client }`; `BackendClient::new(base_url)`; `async fn get_settings(&self) -> Result<Settings>` (GET /api/settings); `async fn list_connections(&self) -> Result<Vec<Connection>>`; `async fn is_ready(&self) -> bool` (1s-timeout GET /api/settings → is_success). `thiserror` enum ClientError with status + URL context; 5s default request timeout.
- lib.rs re-exports + unit tests against an in-process stub (tokio TcpListener writing canned 200 JSON): get_settings parses; list_connections parses a 2-element array; is_ready false on connection-refused.
  </action>
  <verify>
    <automated>cd desktop-gpui && cargo test -p webterm-backend-client</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - backend-client/src/rest.rs contains "/api/settings" and "/api/connections"
    - types.rs Connection field names match JSON tags in be/internal/db/models.go for included fields
    - is_ready returns false on connection-refused (asserted by test)
  </acceptance_criteria>
  <done>Typed client proven against stubs; ready for Phase 22/23 expansion</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd desktop-gpui && cargo test --workspace` green
- [ ] Settings key-custody guarantees verified by tests (stability, format, permissions path)
</verification>

<success_criteria>
- All tasks completed with commits
- Both crates green; supervisor + settings + client form the complete headless core
</success_criteria>

<output>
After completion, create `.planning/phases/20-desktop-foundation-backend-integration/20-02-SUMMARY.md`
</output>
