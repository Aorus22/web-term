---
phase: 20-desktop-foundation-backend-integration
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - desktop-gpui/crates/webterm/src/main.rs
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/src/window_state.rs
  - desktop-gpui/crates/webterm/src/views/settings.rs
files_deleted: []
autonomous: true
requirements: [SHELL-06]
user_setup: []
must_haves:
  truths:
    - "Window bounds (x, y, width, height, maximized) persist across app restart — move/resize/quit/relaunch restores geometry"
  artifacts:
    - path: "desktop-gpui/crates/webterm/src/window_state.rs"
      provides: "window geometry restore + debounced save to DesktopSettings.window_state"
      min_lines: 40
      contains: "WindowState"
    - path: "desktop-gpui/crates/webterm/src/views/settings.rs"
      provides: "minimal settings view: theme selector (synced with sidebar toggle) + backend status line, placeholder note for Phase 26"
      min_lines: 40
  key_links:
    - from: "desktop-gpui/crates/webterm/src/window_state.rs"
      to: "desktop-gpui/crates/settings/src/lib.rs"
      via: "saves WindowState into DesktopSettings (persisted settings.json)"
      pattern: "window_state|save"
    - from: "desktop-gpui/crates/webterm/src/main.rs"
      to: "desktop-gpui/crates/webterm/src/window_state.rs"
      via: "initial window bounds come from restore() reading DesktopSettings"
      pattern: "restore"
---

<objective>
Persist window geometry across restarts and give the Settings nav entry a minimal real view (theme + backend status), completing Phase 20's SHELL-06 requirement and setting the floor for Phase 26's full settings page.

Purpose: SHELL-06 is an explicit Phase 20 success criterion; the minimal settings view keeps the four-entry nav honest.
Output: window_state.rs + views/settings.rs wired into the app.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-desktop-foundation-backend-integration/20-RESEARCH.md
@.planning/phases/20-desktop-foundation-backend-integration/20-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Window-state persistence (restore on open, debounced save on change)</name>
  <files>desktop-gpui/crates/webterm/src/window_state.rs, desktop-gpui/crates/webterm/src/main.rs</files>
  <read_first>desktop-gpui/crates/settings/src/lib.rs, desktop-gpui/crates/webterm/src/main.rs</read_first>
  <action>
Create window_state.rs:
- `restore(settings) -> Option<WindowBounds>`: saved geometry for the initial cx.open_window call (refactor main.rs's settings.window_state read to route through this module so logic lives in one place).
- `observe(window, settings_handle, cx)`: subscribe to the window's geometry/maximized events (gpui 0.2.2 window observation API — verify exact method names on gpui::Window at execution time) and save to DesktopSettings.window_state with a 1s debounce (one save per drag burst, not per pixel); also flush-save on the window close/quit path.

Guards: ignore degenerate geometries (zero width/height — minimized windows can report 0x0); skip saves when values are unchanged.
  </action>
  <verify>
    <automated>cd desktop-gpui && cargo build -p webterm</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -c "debounce\|Duration::from" desktop-gpui/crates/webterm/src/window_state.rs</automated>
    <fails_when>count is 0 (debounce timing absent)</fails_when>
  </verify>
  <acceptance_criteria>
    - window_state.rs contains a debounce timing constant and writes DesktopSettings.window_state
    - main.rs uses restore() for initial window bounds
    - build passes
  </acceptance_criteria>
  <done>Geometry persists (manual check at phase close-out)</done>
</task>

<task type="auto">
  <name>Task 2: Minimal Settings view (theme + backend status)</name>
  <files>desktop-gpui/crates/webterm/src/views/settings.rs, desktop-gpui/crates/webterm/src/app_state.rs</files>
  <read_first>desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/webterm/src/theme.rs</read_first>
  <action>
Replace the Settings placeholder from 20-03 with views/settings.rs: current theme selector (same control as the sidebar toggle — kept in sync through AppState), backend status line (display "Running (local)" WITHOUT the port — plus which backend_path is in use), and a note that full settings arrive in Phase 26. No new settings fields here (SET-01 is Phase 26); the view only surfaces existing state.

Update app_state.rs View::Settings route to render it.
  </action>
  <verify>
    <automated>cd desktop-gpui && cargo build -p webterm && cargo clippy -p webterm -- -D warnings</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - views/settings.rs exists and is rendered by the Settings nav route
    - the view does not display the backend port (host-only/label display)
    - build + clippy pass
  </acceptance_criteria>
  <done>Settings nav shows real state; Phase 20 scope complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo build --workspace + cargo test --workspace green
- [ ] clippy clean for touched crates
- [ ] Manual: move/resize window, quit, relaunch → geometry restored (phase close-out checkpoint)
</verification>

<success_criteria>
- All tasks completed with commits
- Window geometry persistence implemented with debounce + degenerate-state guard
</success_criteria>

<output>
After completion, create `.planning/phases/20-desktop-foundation-backend-integration/20-04-SUMMARY.md`
</output>
