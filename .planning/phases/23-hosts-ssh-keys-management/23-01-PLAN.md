---
phase: 23-hosts-ssh-keys-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop-gpui/crates/backend-client/src/types.rs
  - desktop-gpui/crates/backend-client/src/rest.rs
  - desktop-gpui/crates/backend-client/src/lib.rs
  - desktop-gpui/crates/backend-client/tests/connection_api_test.rs
files_deleted: []
autonomous: true
requirements: [HOSTS-01, HOSTS-04, KEYS-01]
user_setup: []
must_haves:
  truths:
    - "BackendClient can create, read, update, and delete connections via REST API"
    - "BackendClient can export connections as JSON array and import connections payload"
    - "BackendClient can list, get, create, and delete SSH keys via REST API"
    - "Contract test suite validates serialization and HTTP wire protocol against mock server"
  artifacts:
    - path: "desktop-gpui/crates/backend-client/src/types.rs"
      provides: "SshKey, CreateConnectionRequest, UpdateConnectionRequest, CreateKeyRequest, ImportResult DTOs"
      min_lines: 90
    - path: "desktop-gpui/crates/backend-client/tests/connection_api_test.rs"
      provides: "Comprehensive REST API test suite for Connections and SSH Keys"
      min_lines: 100
  key_links:
    - from: "desktop-gpui/crates/backend-client/src/rest.rs"
      to: "desktop-gpui/crates/backend-client/src/types.rs"
      via: "DTO serialization for REST calls"
      pattern: "CreateConnectionRequest|SshKey"
---

<objective>
Implement typed REST client methods and DTOs in `webterm-backend-client` for complete Connection management (CRUD, Export, Import) and SSH Key management (List, Create, Delete), backed by a contract test suite.

Purpose: Provide robust, typed primitives for the GPUI desktop UI to manage saved hosts and keys without duplicating serialization logic.
Output: Extended `types.rs`, `rest.rs`, and new integration test `tests/connection_api_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-hosts-ssh-keys-management/23-CONTEXT.md
@.planning/phases/23-hosts-ssh-keys-management/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Add DTOs and BackendClient methods for Connections and SSH Keys</name>
<files>desktop-gpui/crates/backend-client/src/types.rs, desktop-gpui/crates/backend-client/src/rest.rs, desktop-gpui/crates/backend-client/src/lib.rs</files>
<read_first>desktop-gpui/crates/backend-client/src/types.rs, desktop-gpui/crates/backend-client/src/rest.rs, be/internal/api/connections.go, be/internal/api/keys.go, be/internal/api/export.go</read_first>
<action>
1. In `desktop-gpui/crates/backend-client/src/types.rs`:
   - Define `SshKey` DTO:
     - `id: String`, `name: String`, `key_type: String`, `fingerprint: String`, `created_at: Option<String>`, `updated_at: Option<String>`.
   - Define `CreateKeyRequest`:
     - `name: String`, `key_base64: String`.
   - Define `CreateConnectionRequest`:
     - `label: String`, `host: String`, `port: u16`, `username: String`, `password: Option<String>`, `tags: Vec<String>`, `auth_method: String`, `ssh_key_id: Option<String>`.
   - Define `UpdateConnectionRequest`:
     - Same fields as create.
   - Define `ImportResult`:
     - `imported: usize`, `skipped: usize`.
2. In `desktop-gpui/crates/backend-client/src/rest.rs`:
   - Implement `get_connection(&self, id: &str) -> Result<Connection, ClientError>`.
   - Implement `create_connection(&self, req: &CreateConnectionRequest) -> Result<Connection, ClientError>`.
   - Implement `update_connection(&self, id: &str, req: &UpdateConnectionRequest) -> Result<Connection, ClientError>`.
   - Implement `delete_connection(&self, id: &str) -> Result<(), ClientError>`.
   - Implement `export_connections(&self) -> Result<Vec<Connection>, ClientError>`.
   - Implement `import_connections(&self, req: &[Connection]) -> Result<ImportResult, ClientError>`.
   - Implement `list_keys(&self) -> Result<Vec<SshKey>, ClientError>`.
   - Implement `get_key(&self, id: &str) -> Result<SshKey, ClientError>`.
   - Implement `create_key(&self, req: &CreateKeyRequest) -> Result<SshKey, ClientError>`.
   - Implement `delete_key(&self, id: &str) -> Result<(), ClientError>`.
3. In `desktop-gpui/crates/backend-client/src/lib.rs`:
   - Export the new types.
</action>
<verify>
<automated>cargo check -p webterm-backend-client --manifest-path desktop-gpui/Cargo.toml</automated>
<fails_when>non-zero exit</fails_when>
</verify>
<acceptance_criteria>
- All DTOs and client methods compile cleanly without warnings
</acceptance_criteria>
<done>Connections and SSH Keys DTOs and REST client methods implemented</done>
</task>

<task type="auto">
<name>Task 2: Create comprehensive contract test suite for Connection & Key REST APIs</name>
<files>desktop-gpui/crates/backend-client/tests/connection_api_test.rs</files>
<read_first>desktop-gpui/crates/backend-client/tests/terminal_ws_test.rs</read_first>
<action>
1. Create `desktop-gpui/crates/backend-client/tests/connection_api_test.rs`:
   - Spawn a mock `TcpListener` with custom HTTP request/response handlers for:
     - `GET /api/connections` -> returns connection list JSON.
     - `GET /api/connections/c1` -> returns single connection JSON.
     - `POST /api/connections` -> validates request JSON body, returns 201 Created with new connection.
     - `PUT /api/connections/c1` -> validates update JSON body, returns updated connection.
     - `DELETE /api/connections/c1` -> returns 204 No Content.
     - `GET /api/connections/export` -> returns array of connections.
     - `POST /api/connections/import` -> validates import payload, returns `{"imported": 2, "skipped": 1}`.
     - `GET /api/keys` -> returns list of keys with types and fingerprints.
     - `POST /api/keys` -> validates `key_base64`, returns 201 Created with new key.
     - `DELETE /api/keys/k1` -> returns 204 No Content.
2. Run tests to ensure complete coverage.
</action>
<verify>
<automated>cargo test -p webterm-backend-client --test connection_api_test --manifest-path desktop-gpui/Cargo.toml</automated>
<fails_when>non-zero exit</fails_when>
<automated>cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings</automated>
<fails_when>non-zero exit</fails_when>
</verify>
<acceptance_criteria>
- `connection_api_test` passes all tests
- 0 warnings across workspace from clippy
</acceptance_criteria>
<done>Connection and SSH Key REST endpoints verified by contract tests</done>
</task>

</tasks>
