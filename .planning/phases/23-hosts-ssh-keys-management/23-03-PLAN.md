---
phase: 23-hosts-ssh-keys-management
plan: 03
type: execute
wave: 3
depends_on: [23-01, 23-02]
files_modified:
  - desktop-gpui/crates/webterm/src/app_state.rs
  - desktop-gpui/crates/webterm/src/views/connection_modal.rs
  - desktop-gpui/crates/webterm/src/views/mod.rs
  - desktop-gpui/crates/webterm/src/views/nav.rs
  - desktop-gpui/crates/webterm/tests/connection_modal_test.rs
files_deleted: []
autonomous: true
requirements: [HOSTS-01, HOSTS-04, KEYS-03]
user_setup: []
must_haves:
  truths:
    - "User can create, edit, and delete SSH connections via modal form"
    - "User can choose between Password and SSH Key authentication per connection"
    - "User can export connections to JSON compatible with web app"
    - "User can import connections from JSON payload and see imported/skipped count"
  artifacts:
    - path: "desktop-gpui/crates/webterm/src/views/connection_modal.rs"
      provides: "Connection Create/Edit modal dialog with auth method toggle and tag inputs"
      min_lines: 150
    - path: "desktop-gpui/crates/webterm/tests/connection_modal_test.rs"
      provides: "Tests for connection form validation and JSON export/import payload roundtrip"
      min_lines: 60
  key_links:
    - from: "desktop-gpui/crates/webterm/src/views/nav.rs"
      to: "desktop-gpui/crates/webterm/src/views/connection_modal.rs"
      via: "renders connection modal overlay when show_connection_modal is Some"
      pattern: "render_connection_modal"
---

<objective>
Implement the Connection Create/Edit modal dialog (`HOSTS-01`, `KEYS-03`) and JSON Import/Export integration (`HOSTS-04`), allowing full configuration of SSH hosts, authentication method selection, and backup roundtrips.

Purpose: Deliver complete lifecycle parity for SSH connection records in the GPUI desktop app.
Output: `views/connection_modal.rs`, updated `app_state.rs` and `nav.rs`, and tests in `tests/connection_modal_test.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-hosts-ssh-keys-management/23-CONTEXT.md
@.planning/phases/23-hosts-ssh-keys-management/23-RESEARCH.md
@.planning/phases/23-hosts-ssh-keys-management/23-01-SUMMARY.md
@.planning/phases/23-hosts-ssh-keys-management/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Implement ConnectionModal and Import/Export state & actions</name>
<files>desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/webterm/src/views/connection_modal.rs, desktop-gpui/crates/webterm/src/views/mod.rs, desktop-gpui/crates/webterm/src/views/nav.rs</files>
<read_first>desktop-gpui/crates/webterm/src/app_state.rs, desktop-gpui/crates/backend-client/src/types.rs</read_first>
<action>
1. In `desktop-gpui/crates/webterm/src/app_state.rs`:
   - Define `ConnectionModalMode`: `Create` or `Edit(String)`.
   - Define `ConnectionFormState`:
     - `label: String`, `host: String`, `port: String`, `username: String`, `password: String`, `tags: String`, `auth_method: String` ("password" or "key"), `ssh_key_id: Option<String>`.
   - Add fields to `AppState`:
     - `pub connection_modal: Option<ConnectionFormState>`.
     - `pub import_modal: bool`.
     - `pub import_payload: String`.
     - `pub notification: Option<String>`.
   - Add methods to `AppState`:
     - `pub fn open_create_connection_modal(&mut self, cx: &mut Context<Self>)`.
     - `pub fn open_edit_connection_modal(&mut self, id: &str, cx: &mut Context<Self>)`.
     - `pub fn close_connection_modal(&mut self, cx: &mut Context<Self>)`.
     - `pub fn save_connection_form(&mut self, cx: &mut Context<Self>)`: calls `client.create_connection` or `client.update_connection`, reloads connections, and closes modal.
     - `pub fn delete_connection(&mut self, id: &str, cx: &mut Context<Self>)`: calls `client.delete_connection`, reloads connections.
     - `pub fn export_connections_to_disk(&mut self, cx: &mut Context<Self>)`.
     - `pub fn submit_import_connections(&mut self, cx: &mut Context<Self>)`.
2. Create `desktop-gpui/crates/webterm/src/views/connection_modal.rs`:
   - Centered modal dialog:
     - Header: "Add New Host" / "Edit Host".
     - Label input field.
     - Host and Port input fields.
     - Username input field.
     - Auth method toggle buttons: "🔑 SSH Key" vs "🔒 Password".
     - If "Password": Password input field.
     - If "SSH Key": Key selector dropdown/options from `app.ssh_keys`.
     - Tags input field (comma-separated).
     - Action buttons: "Cancel" and "Save Host".
3. In `desktop-gpui/crates/webterm/src/views/nav.rs`:
   - Render `render_connection_modal` overlay when `app.connection_modal.is_some()`.
</action>
<verify>
<automated>cargo check -p webterm --manifest-path desktop-gpui/Cargo.toml</automated>
<fails_when>non-zero exit</fails_when>
</verify>
<acceptance_criteria>
- Connection modal compiles cleanly and handles Create, Edit, Delete, Export, Import
</acceptance_criteria>
<done>Connection modal and export/import handlers implemented</done>
</task>

<task type="auto">
<name>Task 2: Tests for Connection form validation and Import/Export JSON compatibility</name>
<files>desktop-gpui/crates/webterm/tests/connection_modal_test.rs</files>
<read_first>desktop-gpui/crates/backend-client/src/types.rs</read_first>
<action>
1. Create `desktop-gpui/crates/webterm/tests/connection_modal_test.rs`:
   - Test form conversion to `CreateConnectionRequest` and `UpdateConnectionRequest`:
     - Default port 22 when input empty or invalid.
     - Trimming whitespace from label, host, username.
     - Parsing comma-separated tags into `Vec<String>`.
     - Validating required fields (label, host, username).
   - Test JSON Export/Import structure:
     - Verify serialized `Vec<Connection>` matches schema expected by backend `POST /api/connections/import`.
     - Verify import response parsing (`imported`, `skipped`).
</action>
<verify>
<automated>cargo test -p webterm --test connection_modal_test --manifest-path desktop-gpui/Cargo.toml</automated>
<fails_when>non-zero exit</fails_when>
<automated>cargo clippy --workspace --manifest-path desktop-gpui/Cargo.toml -- -D warnings</automated>
<fails_when>non-zero exit</fails_when>
</verify>
<acceptance_criteria>
- `connection_modal_test` passes all tests
- 0 warnings across workspace from clippy
</acceptance_criteria>
<done>Connection form validation and import/export compatibility verified</done>
</task>

</tasks>
