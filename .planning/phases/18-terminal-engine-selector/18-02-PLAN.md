---
phase: 18-terminal-engine-selector
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: [fe/src/features/terminal/components/WTermEngine.tsx, fe/src/features/terminal/components/XTermEngine.tsx, fe/src/features/terminal/components/TerminalWrapper.tsx]
autonomous: true
requirements: [ENGINE-03]

must_haves:
  truths:
    - "WTermEngine renders @wterm/react and handles mouse events"
    - "XTermEngine renders xterm.js and its addons"
    - "TerminalWrapper switches engines based on settings"
  artifacts:
    - path: "fe/src/features/terminal/components/WTermEngine.tsx"
      provides: "wterm implementation"
    - path: "fe/src/features/terminal/components/XTermEngine.tsx"
      provides: "xterm.js implementation"
    - path: "fe/src/features/terminal/components/TerminalWrapper.tsx"
      provides: "Engine selection logic"
---

<objective>
Implement the specific terminal engine components (WTerm and XTerm) and a wrapper that abstracts them for use in the main terminal pane.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-terminal-engine-selector/18-RESEARCH.md
@fe/src/features/terminal/TerminalPane.tsx
@fe/src/features/terminal/useTerminalMouse.ts
</context>

<tasks>

<task type="auto">
  <name>Create WTermEngine Component</name>
  <files>fe/src/features/terminal/components/WTermEngine.tsx</files>
  <action>
    - Create `WTermEngine.tsx` as a `forwardRef` component.
    - Extract `@wterm/react` implementation from `TerminalPane.tsx`.
    - Use `useTerminalMouse` inside this component.
    - Expose `write` and `focus` via `useImperativeHandle`.
  </action>
  <verify>
    <automated>cd fe && npx tsc --noEmit</automated>
  </verify>
  <done>WTermEngine component created with legacy @wterm/react logic.</done>
</task>

<task type="auto">
  <name>Create XTermEngine Component</name>
  <files>fe/src/features/terminal/components/XTermEngine.tsx</files>
  <action>
    - Create `XTermEngine.tsx` as a `forwardRef` component.
    - Implement using `@xterm/xterm`, `@xterm/addon-fit`, `@xterm/addon-web-links`, and `@xterm/addon-webgl`.
    - Import `@xterm/xterm/css/xterm.css`.
    - Handle `fontFamily`, `fontSize`, `theme`, and `cursorBlink` props.
    - Expose `write` and `focus` via `useImperativeHandle`.
    - Note: xterm.js handles mouse tracking natively, do not use `useTerminalMouse`.
  </action>
  <verify>
    <automated>cd fe && npx tsc --noEmit</automated>
  </verify>
  <done>XTermEngine component created using xterm.js.</done>
</task>

<task type="auto">
  <name>Create TerminalWrapper Component</name>
  <files>fe/src/features/terminal/components/TerminalWrapper.tsx</files>
  <action>
    - Create `TerminalWrapper.tsx` that takes props similar to the original `Terminal` but also receives the `engine` type ('wterm' | 'xterm').
    - Conditionally render either `WTermEngine` or `XTermEngine`.
    - Forward the ref to the active engine.
  </action>
  <verify>
    <automated>cd fe && npx tsc --noEmit</automated>
  </verify>
  <done>TerminalWrapper component created to manage engine selection.</done>
</task>

</tasks>

<success_criteria>
- All three components are correctly typed and exported.
- Both engines satisfy the `TerminalHandle` interface via their refs.
</success_criteria>

<output>
After completion, create `.planning/phases/18-terminal-engine-selector/18-02-SUMMARY.md`
</output>
