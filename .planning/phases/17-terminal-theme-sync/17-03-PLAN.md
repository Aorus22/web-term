---
phase: 17-terminal-theme-sync
plan: 03
type: execute
wave: 1
depends_on: [17-02]
files_modified:
  - fe/src/components/AppThemeProvider.tsx
  - fe/src/components/TabBar.tsx
  - fe/src/components/NewTabView.tsx
  - fe/src/features/sftp/components/FileIcon.tsx
  - fe/src/features/sftp/components/TransferManager.tsx
autonomous: true
requirements:
  - THEME-05
gap_closure: true

must_haves:
  truths:
    - "UI colors respond to terminal theme changes"
    - "Status indicators (connected/connecting/error) use theme colors"
    - "File type icons use theme colors"
    - "UI appears cohesive and tidy across all terminal themes"
  artifacts:
    - path: "fe/src/components/AppThemeProvider.tsx"
      provides: "Status color CSS variables"
      new_vars: ["--status-connected", "--status-connecting", "--status-error"]
    - path: "fe/src/components/TabBar.tsx"
      provides: "Theme-responsive status indicators"
      replaces: ["green-500", "yellow-500", "destructive"]
    - path: "fe/src/components/NewTabView.tsx"
      provides: "Theme-responsive connection status"
      replaces: ["bg-green-500"]
    - path: "fe/src/features/sftp/components/FileIcon.tsx"
      provides: "Theme-responsive file type colors"
      replaces: ["text-blue-500", "text-purple-500", etc.]
  key_links:
    - from: "AppThemeProvider.tsx"
      to: "TabBar.tsx"
      via: "CSS variable --status-connected"
      pattern: "--status-connected"
    - from: "AppThemeProvider.tsx"
      to: "FileIcon.tsx"
      via: "CSS variable --file-folder, --file-text, etc."
      pattern: "--file-folder"
---

<objective>
Fix UI visual inconsistency by replacing hardcoded colors with theme-responsive CSS variables.

Purpose: The UI appears "messy" because status indicators (connected = green, connecting = yellow) and file icons use fixed Tailwind colors that don't adapt to terminal themes. This gap closure replaces hardcoded colors with semantic CSS variables.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-terminal-theme-sync/17-UAT.md
@.planning/phases/17-terminal-theme-sync/17-01-SUMMARY.md
@.planning/phases/17-terminal-theme-sync/17-02-SUMMARY.md
@fe/src/components/AppThemeProvider.tsx
@fe/src/components/TabBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic status color variables to AppThemeProvider</name>
  <files>fe/src/components/AppThemeProvider.tsx</files>
  <action>
Add semantic status color variables to AppThemeProvider that map terminal colors to semantic meanings:

1. Add `--status-connected` = use green color from terminal theme (or compute from green/cyan if available)
2. Add `--status-connecting` = use yellow color (or yellow from terminal theme if available)
3. Add `--status-disconnected` = use `--muted-foreground`
4. Add `--status-error` = use red from terminal theme (or compute)

Also add semantic file type colors:
- `--file-folder` = yellow/gold equivalent from terminal theme
- `--file-text` = blue equivalent from terminal theme
- `--file-code` = green equivalent
- `--file-archive` = orange equivalent
- `--file-image` = purple equivalent

Use existing terminal theme colors where available, or compute appropriate values based on luminance.

Per Gap (17-UAT.md): "UI should be visually consistent and aesthetically pleasing when terminal theme is applied"
  </action>
  <verify>grep -c "status-connected" fe/src/components/AppThemeProvider.tsx</verify>
  <done>AppThemeProvider exports --status-connected, --status-connecting, --status-disconnected, --status-error CSS variables</done>
</task>

<task type="auto">
  <name>Task 2: Update TabBar status indicators to use CSS variables</name>
  <files>fe/src/components/TabBar.tsx</files>
  <action>
Replace hardcoded Tailwind colors in TabBar.tsx with CSS variable equivalents:

1. Replace `fill-green-500 text-green-500` with CSS variable for connected status
2. Replace `fill-yellow-500 text-yellow-500` with CSS variable for connecting status
3. Replace `text-destructive` with CSS variable for error status
4. Replace `text-muted-foreground` background patterns

Use inline styles with CSS variables like: `style={{ color: 'var(--status-connected)', backgroundColor: 'var(--status-connected)' }}` or use the CSS variables directly in Tailwind arbitrary values: `[var(--status-connected)]`

Per Gap: User reported "UI is messy" because status dots use fixed green/yellow colors
  </action>
  <verify>grep -c "green-500\|yellow-500" fe/src/components/TabBar.tsx</verify>
  <done>TabBar status indicators respond to terminal theme changes</done>
</task>

<task type="auto">
  <name>Task 3: Update NewTabView connection status indicator</name>
<files>fe/src/components/NewTabView.tsx</files>
<action>
Replace hardcoded `bg-green-500` in NewTabView.tsx with CSS variable for status:
- Replace `bg-green-500 animate-pulse` with `var(--status-connected)` or equivalent

Per Gap: Active connection indicator uses fixed green color
  </action>
<verify>grep -c "bg-green-500" fe/src/components/NewTabView.tsx</verify>
<done>NewTabView connection status indicator uses theme colors</done>
</task>

<task type="auto">
  <name>Task 4: Update FileIcon and TransferManager to use theme colors</name>
  <files>fe/src/features/sftp/components/FileIcon.tsx, fe/src/features/sftp/components/TransferManager.tsx</files>
  <action>
Update FileIcon.tsx to replace hardcoded text colors with semantic CSS variables:
- Replace `text-blue-500`, `text-purple-500`, `text-orange-500`, etc. with `--file-text`, `--file-code`, `--file-archive`, `--file-image` variables
- Use `--file-folder` for folder icons

Update TransferManager.tsx to replace `text-green-500` with `--status-connected` CSS variable.

Per Gap: File type icons and transfer status use fixed colors
  </action>
<verify>grep -c "green-500\|blue-500\|purple-500\|orange-500" fe/src/features/sftp/components/FileIcon.tsx fe/src/features/sftp/components/TransferManager.tsx</verify>
<done>File icons and transfer status use theme colors</done>
</task>

<task type="auto">
  <name>Task 5: Verify all theme colors are CSS variable based</name>
<files>fe/src</files>
<action>
Run a final verification across the codebase to ensure no hardcoded status colors remain that would break theme consistency:
- Search for `green-500`, `yellow-500`, `red-500`, `blue-500` in status-indicator contexts
- Verify those remaining are intentional (non-status, non-theme-dependent contexts) or add to scope

This is a catch-all to ensure full coverage.
  </action>
<verify>grep -r "fill-green-500\|fill-yellow-500\|text-green-500\|bg-green-500" fe/src --include="*.tsx" | grep -v "var\|--status"</verify>
<done>All status-related colors use CSS variables</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {user→app} | No external input - CSS variable updates are internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | CSS variable injection | N/A | AppThemeProvider validates hex colors (existing: isValidHex) |
| T-17-02 | Information | Color exposure | Accept | Status colors are non-sensitive terminal state |
</threat_model>

<verification>
- [ ] Status indicators (connected/connecting/error) use theme-responsive variables
- [ ] File type icons respond to terminal theme changes
- [ ] No fixed Tailwind colors (green-500/yellow-500) remain in status indicators
- [ ] UI appears cohesive across Nord, One Dark, Solarized Light themes
</verification>

<success_criteria>
Gap closed: "UI should be visually consistent and aesthetically pleasing when terminal theme is applied"

- All status indicators use `--status-*` CSS variables
- All file type icons use `--file-*` CSS variables  
- Grep shows no hardcoded green-500/yellow-500 in status contexts after fix
- User verification confirms UI is "tidy" across themes
</success_criteria>

<output>
After completion, create `.planning/phases/17-terminal-theme-sync/17-03-SUMMARY.md`
</output>